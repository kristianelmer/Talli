# Python Domain and Filing Engine First

The first implementation target will be a Python domain and filing engine, with the web UI stack deferred until the filing core is proven. Python fits the user's familiarity and the product's early needs: deterministic filing models, validation, XML/JSON/PDF/spreadsheet transformations, public-data validation, and fixture-heavy tests.
