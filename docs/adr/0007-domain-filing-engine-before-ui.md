# Domain and Filing Engine Before UI

When implementation begins, the first milestone will be the domain model, narrow ledger, holding actions, year-end interview data model, filing readiness gate, filing simulation outputs, and public-data validation harness. A polished UI comes later because the core risk is filing accuracy and support-boundary validation, not interface rendering.
