# RF-1086 TT02 acceptance — 2026-07-14

Status: accepted test evidence; production remains disabled
Code under test: `1d06b06`
Machine evidence: `rf1086-tt02-2026-07-14.json`

Talli acquired a Maskinporten system-user token for the Tenor synthetic company
`LOGISK ØDE TIGER AS` (`310279617`) and scope
`skatteetaten:innrapporteringaksjonaerregisteroppgave`. The token had a 119-second
lifetime and was used in memory only.

The test run submitted the 2025 no-activity launch shape in the official order:

1. hovedskjema accepted;
2. one underskjema accepted;
3. confirmation accepted with delivery, dialog, and transmission references;
4. archive retrieval returned the exact two submitted XML documents.

The repository evidence contains only authority references, UUID idempotency
keys, and SHA-256 hashes. It excludes the access token, private key, synthetic
personal identifier, and raw XML. The ignored local evidence journal retains
the resumable call state but is not a production secret store.

The first archive lookup immediately after confirmation returned
`GLD_021 / GLD_1017`; the documented `forsendelseId` succeeded when polled
later and returned both documents. The client handles only that specific
observed eventual-consistency condition as a retry. Other 404s remain blocked.

This closes the RF-1086 authority-access and TT02 submission evidence rows. It
does not approve production credentials, public direct-filing claims, or the
production feature switch. Those remain behind the named reviewer, security,
restore, billing, runtime-record, and launch-signoff gates.
