[project]
name = "talli-backend"
version = "0.1.0"
description = "Talli FastAPI production boundary"
requires-python = ">=3.12,<3.14"
dependencies = [
  "fastapi==0.116.1",
  "psycopg[binary]==3.2.9",
  "pydantic==2.11.7",
  "reportlab==5.0.0",
  "uvicorn==0.35.0",
]

[dependency-groups]
dev = [
  "httpx==0.28.1",
  "pytest==8.4.1",
]

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/talli_backend"]

[tool.hatch.build.targets.wheel.force-include]
"../../holding_core/assets/fonts/NotoSans-Regular.ttf" = "talli_backend/modules/corporate_governance/assets/fonts/NotoSans-Regular.ttf"
"../../holding_core/assets/fonts/NotoSans-Bold.ttf" = "talli_backend/modules/corporate_governance/assets/fonts/NotoSans-Bold.ttf"
"../../holding_core/assets/fonts/OFL.txt" = "talli_backend/modules/corporate_governance/assets/fonts/OFL.txt"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
