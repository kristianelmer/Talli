# Bounded archive source/tracker review

Verdict: no material findings in the reviewed mutable-source delta. This is a source review, not database or complete-gate evidence.

## Coverage and conclusions

- `architecture/company-archive-sources.json:5–12` replaces only the two retired physical opening source names with the two RF-owned sources, adds separately qualified `ledger.opening_bank_inputs`, and records the five canonical RF archive sources alongside the unchanged five generic sibling sources. An independent JSON comparison confirms exactly two removals/eight additions, no changed retained source definition, and all five generic source scopes preserved. There is no single-owner alias for combined share/bank facts and no compatibility-baseline or pending16 approval claim.
- `tests/company_access_cancellation_schema.test.mjs:100–113,141–146,181–187,205–208` follows the existing closed source-inventory check: the capability-source set matches the actual adapter read inventory, canonical trigger SQL supplies the new source scopes, retired opening names alone leave the legacy trigger set, and both source calls remain between begin/complete export. The predecessor script-key retarget preserves the same schema test. This review does not independently certify my earlier archive receiver or orchestration implementation.
- `supabase/migrations/20260909190905_shareholder_register_filing_cutover.sql:159` installs the existing BEFORE INSERT/UPDATE/DELETE tracker on the Ledger-owned bank relation with exact year/company columns. Existing `company_archive_track_source_write_v1` locks changed companies before reading/updating generation (`20260808120000_company_access_cancellation_lifecycle.sql:144–215`); `company_archive_complete_export` takes that same lock and rejects changed generation (`:305–321`). A bank-only write after the RF opening therefore invalidates an in-progress export without relying on another share write. The added trigger does not grant runtime DML or change the bank owner/RLS; existing migration-only function-execute borrowing is restored.
- Reverse cutover removes the exact new trigger (`supabase/rollback/20260909190905_shareholder_register_filing_cutover.sql:201`) while restoring predecessor trackers. Reverse contract retains the current bank source/trigger until reverse cutover; full rollback reconciles/restores original opening bank data then drops the Ledger bank relation (`supabase/rollback/20260909190548_shareholder_register_filing_capability.sql:550`). The current contract and both rollback sources remain unchanged by this tracker correction.
- B's two parameterized cases (`apps/backend/tests/test_shareholder_register_filing_lifecycle.py:575–610`) cover the meaningful missing write: record RF opening first, then only bank, check generation +1, force transaction rollback and verify original generation, persist again, read exact `123.45`, reverse the phases, and verify original bank restoration. They preserve the restricted workflow write/read roles. Their execution is pending at this review; no 108-case pass is inferred from the earlier 104-case proof.

## Source binding and limits

Exact base and all 13 reviewed/traced source SHA-256 values are in `/tmp/talli-151-archive-tracking-independent-review.json`. Root/B authored the independently reviewed delta. Reviewer-authored earlier archive/public/runner sources are excluded from independent certification. No repository files were edited, tests run, or DB/provider/hosted calls made. The two acknowledged architecture errors, pending finite16 amendment, fresh owner-journey requirement, full database lane and stage exit remain unchanged.
