# #151 storage, discriminator and consumer inventory

Entry inventory adopted on 9 September 2026 at immutable baseline `91b178c281bcc5fb887a6257d2f72e380199f3e6`. #150 is closed with protected integration; #151 entry evidence is being recorded. No #151 implementation or exit is claimed here. Source references are repository-relative at this baseline unless explicitly historical.

## Ownership baseline

All 13 table families below are still `legacy-business` in `architecture/database-catalog.json`: authority tables at 417/421, generic filing at 491–511, opening at 655/659 and production RF at 671–679. The RF module owns **no table/schema yet** (`apps/backend/src/talli_backend/modules/shareholder_register_filing/module.json:17`). Therefore a facade removal ticket or an RF-shaped column name is not proof that every row in a shared table belongs to RF.

Current registry assignments (`architecture/compatibility.json`): RF facade #151 at 392; readiness/overrides/review facade #149 at 268; annual-accounts evidence writer #153 at 550; broad archive readers #157 at 748. The approved #150 relocation moved exactly two RF provider coordinators, not their table ownership. Its migration is `supabase/migrations/20260909125113_legacy_rf1086_authority_relocation.sql` and its frozen persistence adapter is `apps/backend/src/talli_backend/adapters/postgres_legacy_rf1086_authority.py`.

Source abbreviations used below, all at the candidate above:

- **W** = `supabase/migrations/0001_authenticated_workspace.sql`.
- **T** = `supabase/migrations/0005_company_tax_feedback_persistence.sql` (later changes to shared submission shape).
- **P** = `supabase/migrations/20260715180000_controlled_production_beta.sql`.
- **F** = `supabase/migrations/20260716155621_rf1086_feedback_reconciliation.sql`.
- **L** = `supabase/migrations/20260827100000_ledger_capability.sql`.
- **A** = `apps/web/app/actions.ts`; **S** = `apps/web/app/lib/supabase/server.ts`.
- **R** = `apps/backend/src/talli_backend/adapters/postgres_legacy_rf1086_authority.py`.

## Exact obligation and relationship discriminators

| Table | Actual stored discriminator / binding | Semantic boundary and current consumers |
| --- | --- | --- |
| `public.filing_previews` | W:322: `filing text NOT NULL`, **no allowed-value check**; company/year and optional `setup_id`; XML/text/JSON plus source/creator/time. | Current RF writer A:1289 stores the renderer's `filing` verbatim. Read by simulation A:1332, overrides A:1463, review A:1704, annual readiness A:4715, production approval A:5352, S:873, R:163 and archive. A safe RF selection needs the actual label plus relationship checks, not XML-column presence alone. |
| `public.filing_submissions` | W:338: `filing text NOT NULL`, company/year and `preview_id`. T:21 permits null preview; T:29 requires `(mode=simulation, preview present, authority_test_run_id null)` OR `(mode=test_authority, preview null, authority_test_run_id present)`; adapter mode independently permits simulation/test_authority/production. | RF simulation A:1402 copies preview label/company/year and sets both modes simulation. Tax import T:330/344 requires obligation `skattemelding`, filing **`skattemelding for AS`**, test_authority modes and year 2025. Preserve that sibling row family and its evidence FK. Read by deadline A:1195, annual readiness A:4716, S:890, archive/support. Generic status such as receipt_stored is not production history proof. |
| `public.filing_overrides` | W:390: nonempty free-text `filing`, company/year; optional `preview_id`; free-text `field_target`, old/new/reason, risk, owner confirmation. | #149 facade owns existing generic writes/read. A:1488 copies filing from preview. RF simulation A:1366 selects exact preview filing. Annual readiness instead treats **filing=`aksjonærregisteroppgaven` OR field_target starts `rf1086.`** as RF (`annual-readiness.ts:297`), so conflicting labels/prefixes require explicit handling. No blanket ownership from target prefix is safe. |
| `public.filing_readiness_snapshots` | W:408: `obligation` enum exactly **`aksjonaerregisteroppgaven`, `skattemelding`, `aarsregnskap`**, company/year; UNIQUE(company,year,obligation). Mutable evaluated/updated timestamps and current JSON blocks/warnings. | Existing #149 generic aggregation; A:4786 writes all obligations, A:1340 reads ASCII RF for simulation, A:1196 deadlines, S:1067 and case-bound support. A latest-row table is not immutable versioned readiness/history completeness. #151 must publish RF-owned facts without moving all annual aggregation early. |
| `public.filing_review_comments` | W:424: **no filing or income-year column**; mandatory `preview_id`, company_id; `target` arbitrary text default `rf1086_preview`; severity/body/author/acknowledgment/time. | Year/obligation derive through the preview relationship. A:1712 always writes target `rf1086_preview` even though its lookup at 1704 selects only preview ID/company; the label alone is not authoritative classification. #149 facade writes/adds/acknowledges; RF simulation reads hard blocks at A:1353; S:1084/archive read. |
| `public.authority_permissions` | W:481: the same 3-value ASCII `obligation` enum, company, submitter/confirming user, timestamp, production_enabled; UNIQUE(company,obligation), submitter=confirmed_by. **No income_year.** | A:4905 writes any validated obligation, not just RF. S:1101, A:4714, release/readiness/support/archive consume it. It is owner attestation/permission data, not the AU System User connection table. Registry currently attaches its exact generic scopes to RF #151; that does not assign tax/accounts rows wholesale to RF or make a company-wide row year-specific readiness. |
| `public.authority_test_runs` | W:494: same 3-value ASCII `obligation`, company; environment `test` or `manual_evidence`; accepted/rejected/blocked/pending; references/hash/recorder/time. **No income_year.** | Generic A:4978 writer accepts any validated obligation; annual accounts A:5042 has separate #153 facade; T's imported tax run links to a year-bound submission. S:1118, release/support/archive read. RF connection/preflight versus filing-evidence semantics must be distinguished from actual AU-owned state. Recorded_at year is not the filing year. |
| `public.filing_approval_snapshots` | P:26: CHECK obligation=**`aksjonaerregisteroppgaven`** and profile=**`rf1086_no_activity_v1`**; company/user/year; preview FK + Billing entitlement FK; immutable manifest/hash/payload/adapter and invalidation fields. | RF-only current aggregate. A:5331 calls `approve_production_filing`; S:951/R:154/support read. Retain original identity/hash/relationships, including invalidated history. Partial unique current-preview index P:47. |
| `public.production_filing_submissions` | P:51: same exact RF obligation/profile checks; environment must `production`; company/user/year; unique approval FK, entitlement FK, payload hash; self-FK `supersedes_submission_id`. Status includes unknown/action_required. F:3 adds feedback state/reference/count/time/safe diagnostics and reconciliation lease. | One frozen RF SQL/coordinator writer via R:175 onward; S:952/support read. This is distinct from generic simulation/test rows. A self-FK does not prove implemented or tested correction/replacement semantics. Complete history must include every terminal/unknown row and original approval relationships. |
| `public.production_filing_events` | P:73: **no direct company/year/obligation/actor**; mandatory submission FK supplies context. Operation/state, attempt 1–20, body hash, UUID idempotency, authority reference, failure/result/time; unique(submission,operation,attempt,state). F:88 adds artifact hashes/safe diagnostics. | Frozen journal's prepared/append SQL seams, including #150 relocation's `legacy_rf1086` routines and R:287. Support joins parent submission (`20260830091341_case_bound_support_access.sql:503`). Preserve ordered permanent evidence, not just latest outcome. |
| `public.production_feedback_artifacts` | F:95: company_id and submission FK, **no year/obligation**; Documents ID FK; original provider reference, MIME, 1 byte–10 MiB bound, SHA-256, retrieval time/classification; UNIQUE(submission,sha256), UNIQUE(document_id). | RF owns classification/link evidence; Documents retains generic metadata/blob lifecycle. R:312/325/336 and relocation SQL:628 use published Documents contract. S:956/support read. Derive year/obligation through submission and verify company consistency. Do not move document bytes/table ownership into RF. |

**Existing label divergence to characterize, not normalize during this inventory:** `rf1086.ts:329/336` and `holding_core/rf1086_submission.py:34` emit **`aksjonærregisteroppgaven`** (æ). A:1289 stores it, A:1408 copies it, and annual readiness specifically matches it at `annual-readiness.ts:197`. By contrast, production approval SQL P:334 requires preview.filing **`aksjonaerregisteroppgaven`** (ASCII ae), and obligation fields use that ASCII value. No source migration replacing that comparison was found. This predates #150 and requires an explicit #151 decision/characterization. Do not rewrite a persisted hashed preview/manifest silently, assume the two labels have been normalized in live data, or infer zero RF rows from one literal query.

## Opening data: exact field split and current authority

| Family / fields | Existing meaning and relationships |
| --- | --- |
| `opening_balance_setups` identity | W:194: UUID `id`, company FK, year 2000–2100, UNIQUE(company,year); `locked_at`, `created_by`, `created_at`. There is **no obligation discriminator**: one company-year opening snapshot is consumed across capabilities. Preserve IDs and timestamps needed by previews/archive; do not duplicate the source record into unrelated owners without an explicit projection/identity contract. |
| Share facts in the setup | `share_capital` numeric≥0, `share_count` integer>0, `nominal_value` numeric>0 (W:199–201). RF public `RecordOpeningSnapshotCommand` already owns the semantic command inputs and enforces capital=nominal×count plus shareholder totals (`modules/shareholder_register_filing/public.py:120`). These are the #151-owned source facts to move. |
| Ledger fact in the setup | `bank_balance` numeric≥0 (W:198) is passed separately by `LegacyShareholderRegisterFilingFacade(transaction, ledger_bank_balance)` (`application/shareholder_register_compatibility.py:19`). It is deliberately absent from RF's public opening command. `application/ledger_workflow.py:475` coordinates the frozen snapshot with ledger posting inside the same claimed transaction. Keep ledger monetary/posting authority with ledger; RF must not acquire it because the legacy row also stores this value. |
| `opening_shareholders` | W:233: UUID id, mandatory setup FK CASCADE, company FK; `name`; kind exactly norwegian_person/norwegian_company; nullable NIN 11 digits / org number 9 digits; count integer≥0; created_by/time. No year/obligation field: bind company/year through setup, retain shareholder UUIDs (they key persisted RF subdocuments). SQL writer validates correct primary identifier for kind, trimmed name ≤255, nonnegative counts and total-count equality (L:1023–1051). |

Current authority: `backend_system.record_opening_snapshot_legacy_v1` (L:944; actual inserts 1054/1061) and `list_opening_snapshots_legacy_v1` (L:1082) are frozen compatibility routines owned by `ledger_workflow_store_owner` (L:2172). Its precise SELECT/INSERT grants and owner/member RLS are at L:725/765/778. This is an implementation role, not semantic ownership of shareholder facts. The ledger contract revokes browser/service-role INSERT/UPDATE/DELETE on both tables (`20260827101000_ledger_capability_contract.sql:193`) and removes `ledger.entries.setup_id` after the reference barrier at 191. Do not restore that retired ledger FK when migrating RF.

Consumers: new-year workflow `application/ledger_workflow.py:461`; source reads `adapters/supabase_ledger.py:1840`/record at 2184; Corporate Governance current annual facts `application/corporate_governance_workflow.py:451`; web `S:707` via generated ledger opening-snapshot read; preview A:1268 and annual-readiness A:4677; archive exact company/year setup filter then shareholders by setup IDs (archive route:313/360). Preserve those public/read-only relationships when removing opening compatibility.

## Additional consumers that a physical split must rebind

- Workspace loaders read across all obligations: `apps/web/app/lib/workspace-data.ts:77` and `annual-workspace-server.ts:69`. S:873/890/951/1067/1084/1101/1118 mostly filter **company only**, not obligation/year; presentation does subsequent selection. Replacing only RF action writes leaves these direct reads active.
- `public.company_access_read_support_case` currently returns generic filing, readiness, authority and production evidence under opened-case scopes (`20260830091341_case_bound_support_access.sql:481` and corresponding policies 743–843; public response contract `modules/company_access/public.py:398`). Preserve exact authorized output/redaction through source-owner projections; do not grant raw RF tables to support transport.
- `apps/web/app/archive/[companyId]/[incomeYear]/download/route.ts:285` reads generic submissions by company/year, derives test-run IDs at 295, reads previews at 320, company-wide permission/review rows at 331/335 and opening shareholders by setup IDs at 363. `architecture/company-archive-sources.json:5` declares year-vs-company scopes. This is future #157 facade debt, not authority to delete the exporter now.
- `apps/web/app/lib/backup-restore.ts:5` onward includes these legacy table families in reconstruction inventories. Preserve historical restore semantics and reconcile references; source names in this policy file are not active business writers.
- Leave foreign data with its existing owner: `billing.production_pilot_entitlements` (Billing module:161), `authority_connections.system_user_requests`/`authority_operations` (AU module:68), `public.documents`/Documents evidence references (Documents module:19), and ledger entries/positions (Ledger module:132). RF publishes/consumes contracts around those bindings.

## Decisions still required at #151 entry

1. Record an explicit label/obligation classification and conflict policy using relational provenance. Generic free text, RF target prefixes and preview references can disagree; comments have no year, evidence/permission rows have no year, and production is a separate aggregate. No live distinct-value/count audit was run here, so actual variant prevalence remains unknown.
2. Assign RF row/field ownership in the catalog before exact future-consumer rebindings under ADR0013. Resolve #149 generic readiness/review/override and #151 generic permission/evidence facade boundaries without moving sibling tax/accounts data or deleting future stages' complete tables/scopes.
3. Separate immutable source readiness/history/coverage from mutable legacy readiness and manually recorded evidence. Publish authoritative company/year/obligation completeness and currentness for #149/final #192; never use missing rows, a simulation receipt, or a test-run date as proof no production submission exists.
4. Specify identity-preserving count/hash reconciliation and rollback for opening facts, approved manifests/XML, production event/feedback chains and cross-owner references. Any label correction affecting payload hashes/approved manifests requires explicit behavior/evidence treatment, not a silent backfill rewrite.

This is an entry map only; no storage move or future-stage implementation has begun.
