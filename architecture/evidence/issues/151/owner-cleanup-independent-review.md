# Shared owner/onboarding cleanup draft review

Scoped source pass after one correction. The draft is still unapplied; actual DB proof is pending.

- Independent extraction of the previous cleanup finds 77 explicit deletion families; all remain in the proposed 68 required plus 30 named rollout relations. All98 selected candidates belong to the finite99 engine allowlist. AU/RF/Billing replacements are selected only from named physical tables; views are excluded, required RF/AU/Billing families are checked, and missing ordinary families fail. No facade is recreated and no deletion broadens beyond the supplied company (production events and document links use scoped parent IDs).
- The engine requires loopback and the disposable `postgres` principal with BYPASSRLS, verifies exact table owners, borrows only missing fixture privileges and same-grantor SET membership, and snapshots USER-trigger modes, ACLs and FORCE RLS. Internal FK triggers stay active; FORCE RLS is never disabled. Success restores exact trigger modes/ACL/FORCE and prior schema/membership state before commit. Any operation/restoration failure rolls back the transaction; top-level rollback failure preserves both errors. No application role gains authority.
- The sole finding was the new `backend_system: "postgres"` schema map: the actual schema owner is `ledger_store_owner` (Ledger migration20260827100000:2276). C corrected the unapplied draft to that owner; exact per-table Banking/Ledger/workflow owners remain distinct. This closes the missing-USAGE borrow/revoke path defect.
- Deletion order clears corporate/holding/bank links before referenced Ledger entries, removes receipt/dependent source families, then production artifacts/events/submissions/approvals, pilot before AU request, preparation before opening, and archives before company. Both callers retain the same resource collection/error flow: the annual owner loop and company-onboarding journey. The latter's actual opening+bank fixture families are present. Disabled USER triggers and ACL changes are temporary fixture operations, not product authorization changes.
- The current owner-loop seed explicitly mirrors its synthetic canonical RF permission/preview into the retained public projection, preserving all original fields and verifying whole-row equality. Opening shares and bank use matched identities/timestamps. These are local historical fixtures, not provider evidence or a replacement for a fresh generated owner journey.

Exact draft and source hashes are in `/tmp/talli-151-owner-cleanup-independent-review.json`. No files in the checkout were edited, and no DB/provider calls or tests were run by this reviewer. Mock and actual predecessor/current topology cleanup, success/failure restoration, and both caller proof remain for implementation verification. Pending16 and stage exit are unchanged.
