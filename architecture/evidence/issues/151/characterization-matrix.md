# #151 characterization and retirement matrix — read-only preparation

Entry inventory adopted on 9 September 2026 at immutable baseline `91b178c281bcc5fb887a6257d2f72e380199f3e6`. #150 is closed with protected integration; #151 entry evidence is being recorded. No #151 implementation or exit is claimed here. Source references are repository-relative at this baseline unless explicitly historical.

## Evidence boundary and supported profiles

| Profile or evidence | Existing proof | What is not established |
| --- | --- | --- |
| Persisted web `rf1086_no_activity_v1`: one share class, Norwegian person/company holders, no events | `apps/web/app/lib/rf1086.ts`; five cases in `tests/rf1086_persisted_preview.test.mjs` cover construction, Python rendering, rendering without Python, exact preview/main/subdocument equivalence for one fixture, and mismatched totals blocked in both engines. | These tests do not prove every admitted holder/number/Unicode/order combination or the fresh production approval path. |
| `tests/fixtures/rf1086/no_activity.json` | Python generation, validation, and CLI fixtures in `tests/test_rf1086.py`; historical TT02 evidence below. | Passing a current synthetic fixture is not a new authority acceptance or genuine-company production proof. |
| `stiftelse.json`, `stiftelse_two_founders.json` | Existing Python generation/XSD checks; two-founder 60/40 shares and 18,000/12,000 acquisition allocation. Official example supports formation code N, per `docs/filing/rf1086-code-evidence-register.md`. | The deployed persisted profile is still no-activity. Formation code evidence does not authorize expanding it or establish a TT02 formation acceptance. Preserve existing offline behavior without making a broader live claim. |
| `share_sale.json`, `dividend.json` | Existing offline generation and XSD coverage. Explicit K/S/U live exclusions in `holding_core/rf1086_codes.py` and `tests/test_rf1086_submission.py`. | Schema acceptance does not prove event-code meaning. Purchase/sale/dividend live support remains excluded. |
| `rf1086_invalid/unsupported_share_class.json`, `mismatched_share_count.json` | Existing fail-before-XML validation and CLI failure behavior; blocked messages and public-validation limitations. | Do not turn exclusion/error cases into successful migration fixtures. |
| Historical TT02, code `1d06b06` | `docs/filing/evidence/rf1086-tt02-2026-07-14.{json,md}`: 2025 no-activity, main + one subdocument + confirmation, archived exact submitted XML hashes. Observed GLD_021/GLD_1017 temporary archive failure followed by success is documented. | Not production activation, current deployed end-to-end filing, correction evidence, or support for every local fixture. Preserve its IDs/hashes and sanitized evidence; do not regenerate private XML or claim new provider execution. |
| #150 local contracted backend/browser evidence | `architecture/evidence/issues/150/requirements.json`, RF service/SQL/API suites and `tests/browser_authority_connections.mjs`. Real local auth/RLS/backend/generated client with loopback provider; historical processing → accepted, reload, receipt hash/download, owner denial. | Browser manually seeds the historical approval/submission. It does not generate, approve and send a fresh filing. A Ready nonproduction Preview is not proof of a hosted functioning backend. |

The XSD test in `tests/test_rf1086.py` is conditional on `xmllint`; report its actual run/skip result. Existing July release documentation has historical blocked items, not a present-day completion receipt. The new module remains an opening-snapshot contract shell (`modules/shareholder_register_filing/MODULE.md`) until #151 migrates its owner implementation.

## Exact old/new output characterization

| Surface | Preserve/compare | Existing source and additional meaningful vectors |
| --- | --- | --- |
| Persisted case projection | Company/year/setup identity, previous/current opening facts, holder IDs and order, empty event list, address/city fallbacks, no leakage of ledger bank facts into shareholder policy. | `rf1086.ts:buildNoActivityRf1086Case`; existing single fixture. Add admitted multiple holders, Norwegian company holder, nullable identity alternatives and correct setup/company binding; unsupported classes/foreign holders remain blocked. |
| Readiness and preview | `ready`/`blocked`, issue code/level/order and Norwegian presentation; full preview text; no XML on blocked output. | `holding_core/rf1086.py`, `holding_core/validation.py`, TS renderer. Current test compares one preview exactly but only coarse invalid-case parity. Characterize duplicate/orphan snapshot IDs, totals, invalid identifiers/year, negative/nonfinite values, unsupported events and absent source separately. |
| Statutory document bytes | Exact XML declaration, trailing newline, indentation, self-closing tags, element/attribute/group/orid ordering, escaped text/attributes, six-decimal number formatting and document-to-holder keys. | `holding_core/rf1086.py` and TS renderer. Include ampersand/quotes/æ, mixed-case/non-ASCII/astral IDs, multiple holders, decimal rounding edges and integer-like values. Existing one-holder equality does not settle these cases. Use byte/hash comparisons, not XML semantic equivalence alone. |
| Simulation and original request identity | Payload hash includes filing label/company/year/main/subdocuments; original idempotency and UUIDv5 inputs; request order main → ordered subdocuments → confirmation → archive; body hashes, receipt IDs, feedback IDs and simulated authority marker. | `rf1086-submission.ts`, `holding_core/rf1086_submission.py`, `tests/rf1086_submission_persisted.test.mjs`. Existing exact comparison covers endpoints/body hashes/keys/status and receipt IDs for one holder, not timestamps/full object. Bind a fixed clock for time comparisons. Preserve distinct JS localeCompare/default-key ordering where historical identity requires it; Python `sorted` is not automatically equivalent. |
| Production approval | Immutable company/owner/year/profile/adapter/preview/document hashes, canonical manifest hash, acknowledgement and invalidation/reapproval semantics. | `production-approval.ts`, `tests/production_approval.test.mjs`, approval RPC/action. Regeneration or changed basis must not silently reuse old approval; do not recompute historic hash identity under new spelling/order. |
| Production send/recovery | Original journal and per-operation IDs, preparation before effects, token-before-begin, exact request-first/pilot-second lock order, retry cap, unknown mutation non-replay, historical recovery after eligibility expiry, terminal early return, lease cleanup. | `compatibility/rf1086_authority_workflow.py`; `test_rf1086_compatibility.py`, `test_rf1086_database_runtime.py`, `test_postgres_legacy_rf1086_authority.py`. Absorb this implementation into the owner; no second state machine or new credential path. |
| Feedback/receipt | Acknowledgement never means accepted; explicit classified feedback; completeness/page failures remain unknown/unavailable; original submitted XML hashes and receipt references; Documents stage/finalize/compensation and safe download. | Existing RF compatibility/provider/runtime suites and `tests/production_submission.test.mjs`. Preserve malformed/BOM/strict-JSON/TextEncoder regression cases from #150. |

The old Python standalone submission preparation also references historical paid-billing policy. Preserve it as source characterization where relevant; do not reintroduce that retired commercial policy. The current Billing public entitlement decision remains authoritative.

## Concrete entry discrepancy: display label versus obligation identifier

`apps/web/app/lib/rf1086.ts:329,336` emits `filing: "aksjonærregisteroppgaven"`. `apps/web/app/actions.ts:1295` persists `rendered.filing` unchanged. `approveProductionFiling` builds the ASCII obligation in its manifest (`actions.ts:5372`), then calls `approve_production_filing`. The only checked-in definition, `supabase/migrations/20260715180000_controlled_production_beta.sql:334`, rejects a preview unless `v_preview.filing = 'aksjonaerregisteroppgaven'`.

The #150 browser fixture uses the æ preview label (`tests/browser_authority_connections.mjs:441`) but inserts its historical approval/submission directly. RF runtime fixtures similarly seed their approval basis. Their green historical recovery evidence cannot prove fresh approval admission.

First #151 characterization should run the actual generated-preview → persisted-preview → owner approval sequence on the local canonical migration chain, with all other gates demonstrably satisfied. Capture the resulting denial and prove it is this mismatch, not a different gate. Then resolve the display-label/domain-identifier boundary explicitly within the filing owner, preserving immutable old manifests/payload hashes and existing history. This is a source-confirmed mismatch to reproduce, not a test result produced here or permission to normalize stored history wholesale.

## Full owner journeys needed for the completed stage

1. Supported no-activity company/year: accepted owner opens persisted facts, gets source-backed readiness, generates preview, reviews exact preview, explicitly acknowledges authority and real filing, performs AAL2, receives immutable approval, separately sends through generated API, sees processing, reloads/reconciles to an explicit terminal result, and downloads the exact stored receipt. Use real local auth, PostgreSQL/RLS, API and hydrated web; only provider transport is fake. Do not seed an approval to make this fresh journey pass.
2. Before effects: each missing/stale owner, AAL2, accepted membership, permission/System User/preflight, readiness, hard review block, blocking override, entitlement/pilot/release gate and explicit confirmation independently denies. Confirm no submission row/provider POST. Default absent configuration remains unavailable; local fake production-shaped fixtures do not enable a real provider.
3. Basis and authorization changes: wrong company/year/preview/actor, changed payload/manifest/profile, superseded approval and source change cannot reuse approval. Current authorization is checked at its existing consequential boundaries; errors remain tenant-concealed and actionable.
4. Ambiguous send and recovery: failure before token vs after prepared/provider write; duplicate clicks/concurrent requests; partial main/subdocument progress; unknown response; lease conflict/loss; reloading must preserve original identity and never silently resubmit an uncertain mutation. Historical recovery must remain available after entitlement/approval freshness expires without imposing new-filing eligibility or extra MFA.
5. Feedback: temporary archive delay, pending/unknown, malformed or wrong year/transmission feedback, pagination/completeness failure, accepted/rejected/action-required, duplicate feedback and Documents partial failure. Repeat recovery/reload to prove no duplicate receipts or lost source evidence.
6. Source handoff: immutable company/year/obligation readiness plus complete production history, explicit completeness before claiming no submission, terminal/unknown/correction/source-incident facts, source version/time/attribution. An absent/incomplete source is unavailable. Annual Compliance consumes public facts; Billing alone classifies #177 commercial outcomes. No inferred absence or commercial decision inside RF.
7. Retired topology and presentation: data count/hash and original IDs survive expand/contract/rollback/recutover; direct generic RF writes/old authority paths denied after contract; owner journey still works. Verify Norwegian copy, keyboard operation, 320/768/1440 layout, safe errors/empty/unavailable states, receipt persistence, console/server health and exact fixture/role cleanup.

## Independent work partition after verified entry

- **RF rule/payload owner:** canonical `modules/shareholder_register_filing` rule/render/readiness implementation and exact output corpus; absorb relevant `holding_core/rf1086*` logic and retire the two web Python bridges (`renderRf1086PreviewWithPython`, `simulateRf1086SubmissionWithPython`) plus duplicate TS rules. Coordinate shared `holding_core/models.py` and `holding_cli/main.py` changes: other obligation generators remain frozen. Keep useful golden behavioral tests after temporary dual-engine comparison ends.
- **Persistence/lifecycle owner:** exact RF row/discriminator inventory, owned schema, restricted session/RLS, migrated approval/journal/reconciliation and source projections, expand/contract/rollback proof. Absorb #150 frozen RF compatibility and preserve published Authority Connections, Billing, Company Access and Documents boundaries. Do not blanket-drop shared generic tables still owned by later stages.
- **Root integration owner:** API/public contracts, generated transport, web actions/readers/filing presentation, manifests, compatibility retirement and entry/exit evidence. Exact approved future tuples are in `architecture/evidence/issues/150/rf-credential-relocation-2026-09-09.md`; current RF facade inventory is `architecture/compatibility.json:compat-rf1086-persistence`.
- **Independent verification:** adversarial payload/identity comparison and full fresh owner journey, followed by separate Standards/Spec reviews. Assign tests to reviewers who did not author the corresponding owner module; serialize shared DB/Next work.

The RF standalone test command now lives at `authority_tools/rf1086_test.py`; preserve its exact resumable evidence/TEST gate while replacing its dependency on the RF XML CLI generator under #151. Do not remove private credential grants still required by other approved standalone tools or expand their scope. Absorb/remove active RF compatibility before the #132 stage exit, retaining the approved frozen evidence and statutory bytes. Two linked immutable complete gates and protected integration remain required; this preparation neither waives them nor starts #146 or #193.
