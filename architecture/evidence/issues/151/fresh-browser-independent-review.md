# Independent fresh RF browser and mandatory-runner source review

Scope: the current fixture-authority/cleanup retarget in `tests/browser_shareholder_register_filing.mjs`, its safety tests, the new mandatory package command, final shell phase, and CI registration/order checks. Compared with HEAD `7730bd34`; exact reviewed source hashes and observation time are in `/tmp/talli-151-fresh-browser-independent-review-snapshot.json`. This review made no repository edits and ran no DB, Next, browser, tests, or provider operations.

## Result and closed finding

**Scoped source pass.** The one reported P1 finding is independently closed: `seedFreshBasis` now includes `created_by` in the shareholder INSERT and binds `$4` to the existing `ownerId`. The original NOT NULL/FK contract is satisfied without changing seed scope or granting authority. Reversing exactly those two source replacements reproduces the complete previously reviewed browser-file hash, proving that the browser journey and other fixture behavior are unchanged by this correction.

The new regression executes the actual extracted seed function and checks the opening/shareholder/bank company and owner bindings, common snapshot identity, four prerequisite writes, and absence of seeded preview/approval/journal/feedback success. A reports a confirmed failing regression before the fix and 11 safety tests passing afterward, with zero skips; this reviewer read the corrected source and regression but did not rerun tests or access the DB. The exact final source hashes and linked author receipt are in the final review snapshot. The initial finding/report hashes are retained separately under `/tmp/talli-151-fresh-browser-independent-review-initial*`.

## Scoped checks with no further findings

- The fixture retains the actual browser login/MFA and connection callback, generated backend API, fresh preview, owner review, content approval, send, accepted feedback, repeated read/reload, exact private-download digest, keyboard focus/responsive checks, other-owner concealment, and second-company ambiguous-mutation/no-resend assertions. The main callback is unchanged by the cleanup retarget. No preview, approval, production journal, or provider receipt is inserted by `seedFreshBasis`.
- The six surviving public RF preparation mirrors are declared in the finite helper inventory and deleted for the generated company IDs before the canonical parent opening. Their child order covers retained preview/setup foreign keys. Other-company rows remain outside all deletion predicates.
- The cleanup runs in one finite transaction. The shared helper requires loopback plus the disposable `postgres` BYPASSRLS principal; borrows only missing SET authority and table/schema grants; captures/restores USER trigger enable modes, exact ACLs, and prior membership options; leaves FORCE RLS and internal FK triggers active; rolls back on failure. The separate company-delete savepoint restores Billing receipt DELETE authority without a production grant. This review covers these helper behaviors as used by the RF fixture, not every sibling caller in its expanded allowlist.
- The six technical signoff keys are captured as full JSON rows, changed only for this local fixture, and restored by deleting/reinserting only those keys while original USER triggers are suspended. Missing prior rows remain missing; unrelated keys remain unchanged. Failure recovery is contained in the transaction; teardown aggregates errors rather than hiding them.
- Global production flags remain false. The separately selected test backend accepts only loopback DB/Supabase/mock destinations, uses generated local keys, and sends only the five fixed RF method templates through the loopback mock. Process-wide DNS/socket guards deny external connections. These are test-provider observations, not live provider evidence.
- Both browser contexts retain sanitized warning/error/page-error collection; health/egress assertions follow both closes. Teardown closes browser/process/mock resources and restores role login state and scoped data.
- `test:browser-shareholder-register-filing` includes safety tests plus the full fresh journey with serial test execution. The shell invokes it exactly once after final recutover, successor DB tests, feedback, and the historical authority browser. Required local configuration is asserted rather than converted into a skip. Shell fail-fast behavior and owned generated-file restoration remain registered in executable CI-shim order/failure cases, including failure of this final fresh browser. No earlier lane was dropped.

No broader #151 exit, complete-gate, runtime-pass, hosted/provider, or independent SQL certification is implied. Actual fresh browser execution remains outstanding and cannot be credited from this source review.
