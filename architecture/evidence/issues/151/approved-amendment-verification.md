# Approved RF amendment and exit corrections — 10 September 2026

This continues checkpoint `8f86488cb32e8d50ad8546654e20ea46d9c8885d` after Kristian's exact sixteen-entry approval. It records evolving working-tree verification, not either immutable complete gate or protected integration. The [decision](compatibility-amendment-v2-decision.json) binds the unchanged original proposal and inventory. The immutable compatibility baseline remains byte-identical.

The twelve mixed tuples now belong to the existing Annual Compliance compatibility record. Only the three specified simulation reads and the one mixed archive opening read are retired. The checker requires the finite original tuple set, atomic retirement, exact reviewed operation bodies, unchanged sibling persistence chains/counts and expiry at the last real sibling source. Audit and notification effects retain their original placement. The RF registry record is gone, and the final web Python simulation bridge is removed.

RF actions use the generated canonical contract first, including during overlap when public RF mirrors exist. Only the exact typed RF not-found response permits a sibling fallback. Shared readers merge by identity with canonical precedence and return their existing sanitized failed-source state when RF is unavailable. RF source readiness now includes its owned authority confirmation and production-enabled blocks. These source claims are bounded by the [independent Spec review](approved-amendment-spec-review.md) and [independent checker review](approved-amendment-standards-review.md).

The complete exit audit found additional SQL gaps. Final contract now snapshots classification before parent deletion, reconciles all six generic RF families against canonical rows plus quarantine, removes children before parents with internal foreign keys active, and verifies exact sibling and canonical equality. Temporary migration-only read policies preserve FORCE RLS and are removed before the intended ACL retirement. USER trigger modes are captured and restored so projection housekeeping does not create archive/business effects. Four moved production tables and Annual Compliance readiness snapshots are not deleted.

Canonical-only preparation and scope-admission functions replace their temporary overlap branches at contract. The surviving public barrier only permits sibling writes. Contract rollback explicitly restores the original overlap function definitions. Both rollback forms restore quarantined generic rows in dependency order, reject conflicting identities and malformed original JSON, and preserve immutable evidence. Full rollback now rejects a sibling that reuses an RF UUID and restores archive trigger modes without advancing archive generation. The implementation uses the original function identities and ACLs through `CREATE OR REPLACE`.

Review comments follow their referenced preview's ownership; the predecessor UI's shared `rf1086_preview` target no longer misclassifies valid sibling comments. A sibling override with an RF field prefix is retained as conflicting quarantine evidence, preventing a false complete RF source attestation. The technical inventory reader receives only the missing execution grant for its existing verified-member check. The backend-system migration inventories are synchronized.

The aggregate rehearsal exposed a separate ordering defect: workspace rollback did not restore the published launch-signoff policies before final recutover contracted them again. The runner now includes the existing signoff rollback artifact. A complete simulated phase-sequence regression first reproduced the missing-policy failure; all47 runner tests then passed. The actual fresh aggregate still must rerun in the complete gates.

Focused verification includes56 architecture tests,2,811 backend boundary passes (two pre-existing optional skips and807 database cases excluded),213 plus40 web checks, zero production dependency vulnerabilities, and unchanged generated contracts. These preceded some later SQL/fixture corrections. The final-topology database run passed119 cases before the last two full-rollback corrections; the subsequent54-case lifecycle run passed after those corrections. An additional17-case quarantine run passed after extending parent/dependent replay and malformed-evidence coverage. Counts overlap and are not unique coverage totals.

Both failed and successful transcripts are retained losslessly as gzip files in the [manifest](approved-amendment/manifest.json), with hashes of original and stored bytes. In particular, the first aggregate stopped at an external `node_modules` link (replaced by a checkout-owned `npm ci`), the second passed retained owner/Billing phases but stopped on the signoff-order defect, and a focused final-topology attempt was corrected after two assertions exposed an uncontracted Authority view. None is relabeled as a complete pass.

The fresh hydrated browser now passes the complete original preview/review/approval/Send/receipt and unknown-outcome recovery journeys, tenant concealment, mobile widths, keyboard and egress checks. All12 checks (one journey and11fixture guards) pass without skips. The fixture now supplies the existing restricted Banking login and expects the documented404 concealment response; no product authorization changed. The complete #132 exit envelope, two linked11/11 immutable gates and protected PR/exact-main Release and Preview evidence remain separate acceptance work. #151 and #192 remain open. The serialized source order and production freeze remain in effect; no live provider, paid operation or production activation is claimed.
