# Read-only #151 entry inventory

Entry inventory adopted on 9 September 2026 at immutable baseline `91b178c281bcc5fb887a6257d2f72e380199f3e6`. #150 is closed with protected integration; #151 entry evidence is being recorded. No #151 implementation or exit is claimed here. Source references are repository-relative at this baseline unless explicitly historical.

## Entry authority

- #151 is stage 9, `shareholder_register_filing`. #150 closed on 9 September 2026 after protected integration at the baseline above; its [canonical receipt](https://github.com/kristianelmer/Talli/issues/150#issuecomment-5606893685) preserves the exact predecessor evidence.
- The registry points to RF/#151 and preserves the predecessor immutable pair. The complete clean entry gate passed all eleven checks; the independent receipt validation is recorded in the entry preflight. It is not either final RF gate.
- The approved route continues #151 → #146 → #152 → #153 → #193 [RF → tax → accounts] → #149 → #208 → #192 final → #194. #192 implementation remains paused during source work.

## Current concrete work inventory

| Area | Candidate source and remaining work |
| --- | --- |
| Public capability | `apps/backend/src/talli_backend/modules/shareholder_register_filing/public.py`, `module.json`, `MODULE.md` expose only `RecordOpeningSnapshotCommand`, `ShareholderRegisterFilingCommands.record_opening_snapshot`, immutable opening values and closed errors. Queries/events/ports/tables/schemas are empty. No authoritative RF readiness, history/completeness, correction, or incident contract exists here yet. |
| Opening facts and consumers | `application/shareholder_register_compatibility.py` (`LegacyShareholderRegisterFilingFacade`) and `application/opening_snapshot_compatibility.py` preserve the combined legacy snapshot. `application/ledger_workflow.py` coordinates new-year opening with ledger in one transaction. `adapters/supabase_ledger.py` invokes `backend_system.record_opening_snapshot_legacy_v1` and `list_opening_snapshots_legacy_v1`. Corporate Governance's workflow also consumes this read projection. Migrate shareholder facts without acquiring ledger posting/bank-balance policy; replace these frozen seams through owned public contracts. |
| Statutory rules and bridges | `apps/web/app/lib/rf1086.ts` contains the actual no-activity TypeScript renderer plus `renderRf1086PreviewWithPython` → `holding_cli.main render-rf1086-preview`. `rf1086-submission.ts` contains simulation/planned-call/hash logic plus `simulateRf1086SubmissionWithPython` → `holding_cli.main simulate-rf1086-submission`. Existing Python source is `holding_core/rf1086.py`, `rf1086_submission.py`, `rf1086_codes.py`, `readiness.py`, `models.py` and their validation dependencies. #151 must leave one canonical capability implementation and remove both web CLI bridges and duplicate rules after equivalence. |
| Readiness, review, approval | Remaining web actions in `apps/web/app/actions.ts`: `generateRf1086Preview` (1250), `confirmSimulatedRf1086Submission` (1318), `addFilingOverride` (1449), `addFilingReviewComment` (1681), `acknowledgeFilingReviewComment` (1736), `refreshAnnualReadinessSnapshots` (4638), `confirmAuthorityPermission` (4883), `recordAuthorityTestEvidence` (4933), `approveProductionFiling` (5331). Supporting policy includes `annual-readiness.ts`, `filing-overrides.ts`, `production-approval.ts`, `filing-release-gate.ts`, and owner filing readiness/presentation files. Split RF-owned behavior from other obligations and future Annual Compliance; do not move the whole shared annual aggregator. |
| Already relocated send/recovery | `compatibility/rf1086_authority_workflow.py` is the single frozen journal/state/feedback/coordinator implementation; `adapters/postgres_legacy_rf1086_authority.py` and `adapters/rf1086_authority.py` supply persistence/five fixed HTTP methods. `features/legacy-rf1086` calls only `legacyRf1086SendApprovedFiling` and `legacyRf1086ReconcileFeedback`, via the two `/api/v1/legacy-rf1086/*` routes. #151 absorbs/removes the frozen compatibility owner and retains the two shipped v1 wire contracts while replacing their compatibility implementation. Reuse owned Authority Connections credentials/connection contracts, not a second token or provider implementation. |
| Standalone RF test command | `authority_tools/rf1086_test.py` was separately relocated by approved #150 scope. It remains RF-owned frozen tooling, calls `holding_cli` generation/XSD validation and a fixed JSON-only Node collation helper, and resumes its original evidence journal. #151 must absorb/retire the frozen tool dependency rather than leave it pointing to removed compatibility. It is distinct from the two web CLI bridges. |
| Data and immutable evidence | Enumerate/split RF rows in `public.filing_previews`, `filing_submissions`, `filing_approval_snapshots`, `production_filing_submissions`, `production_filing_events`, `production_feedback_artifacts`; move `opening_shareholders` and shareholder portions of `opening_balance_setups`. Also classify RF portions of generic readiness/review/override/authority-permission/test-evidence records. These remain `legacy-business` catalog entries, not owned RF schema tables. Preserve IDs, relationships, raw XML, payload/document/manifest hashes, event order, timestamps and unknown-state history with count/hash reconciliation. |
| Other consumers | `apps/web/app/lib/supabase/server.ts` still reads filing/production state; owner filing and annual-reporting/review pages present it. `architecture/company-archive-sources.json` and the archive route still read opening/filing evidence under future #157 facade. Audit and notification producers remain later-stage owners. Bind named workflows and public projections; no direct foreign-table writer or capability cycle. |

`compat-rf1086-persistence` has **20 remaining exact scopes** across `actions.ts` and `lib/supabase/server.ts`, removal #151. The eight send/recovery scopes already removed under #150's exact amendment must not be reintroduced. Remaining Annual Compliance and archive scopes are separately frozen. `architecture/compatibility-baseline.json` stays immutable; active records, manifests/catalog/workflows and deterministic dependency graph must match the actual source cutover.

## Source decisions required during #151, not facts supplied by this inventory

1. **RF row and field ownership:** explicitly enumerate each obligation discriminator and the shareholder/ledger split before designing backfill, RLS, foreign keys, reader rebindings and rollback. Define RF readiness/review/override ownership without migrating Annual Compliance or sibling filings early. Preserve generic table families still needed by later owners; remove all RF-authoritative generic rows at exit.
2. **Published source contract:** per #151's approved 9 September source handoff and ADR0013, publish immutable company/year/obligation readiness, complete production-submission history with positive coverage evidence before concluding none submitted, terminal/unknown outcomes, corrections and RF-owned incident facts. Preserve source identity/version, evidence identity, time, attribution and currentness. The package's existing empty query/event surface does not settle those contract shapes. Missing source/coverage means unavailable.
3. **Avoid charging/readiness cycle:** filing workflows receive immutable annual facts through declared workflows; the filing capability must not import Annual Compliance internals. Annual Compliance later aggregates published filing contracts. Readiness used before charging must be independent of paid entitlement, while actual Send keeps mandatory action-time entitlement/MFA/release/pilot gates. Billing alone applies commercial incident classification/refund amounts.
4. **Evidence scope/year and corrections:** existing accepted TT02 evidence is the **2025 no-activity** shape (2026-07-14), with main + one subdocument + confirmation + matching archived hashes. It is historical test evidence, not proof for every year/profile, current provider availability, real-company activation or correction/replacement capability. The deployed renderer's exact pilot profile is no-activity; broader formation and event fixtures are not automatic live support. Keep K/S/U exclusions and untested corrections unavailable unless their separate source/evidence work clears them. Publishing correction/history facts does not itself authorize a new correction writer.

## Required preservation and exit proof

- Characterize fixed inputs/clocks/UUIDs against both TypeScript and existing Python: byte-identical preview/XML, JSON/hash ordering, canonical shareholder IDs, manifest document order, warnings, defaults, coded errors, confirmation and Norwegian presentation. Include existing valid/invalid fixtures in `tests/fixtures/rf1086*`, both official XSDs, code-evidence register and redacted TT02 machine evidence. New source must not silently broaden the no-activity controlled-production profile.
- Keep Send token-before-begin, current owner/fresh filing MFA/exact approval/entitlement/launch controls, immutable per-operation body/key journal, unknown mutation stop and attempt cap. Recovery retains historical rights after entitlement expiry, terminal early return, lease-before-token and finally-release; only fixed GETs, same per-document reads/poll bounds/classification, no blind POST retry. Preserve exact Documents stage/finalize/conditional producer rollback and existing audit effects.
- Retain meaningful coverage presently under `test:rf1086:persisted`, `test:rf1086:submission`, `test:rf1086:production-pilot`, Python RF/authority/API/database suites and the real-backend/local-provider owner browser journey (recovery, reload, receipt hash, other-owner denial, keyboard/responsive and health/egress checks). Enumerate additional complete owner preview/review/approval/Send browser paths needed by #151; current two-operation generated transport is not complete owner filing cutover.
- Run migration count/hash equivalence, genuine restricted-role RLS/tenant/concurrency/idempotency tests, deployment-order and rollback/recutover; preserve one writer and quarantined unknown outcomes. Then remove all RF compatibility implementations, both CLI bridges, temporary equivalence suites/allowlists and obsolete RF generic rows. Update module/docs/catalog/generated clients/Graphify together.
- Require independent review, complete architecture/contract/security/database/browser/build/release envelope and **two consecutive complete gates on immutable revisions** before the following capability begins. Activated production additionally needs seven incident-free days under #132; migration/local evidence does not claim that observation window. Exact protected/main integration receipts remain required by the current project workflow.

Graphify was queried without rebuild/save-result. Its existing snapshot identifies the RF Python/TS/CLI relationships but also still lists a now-retired `scripts/rf1086-authority-test.mjs`; current candidate source/manifests, not stale graph edges, ground this inventory. No graph or repository output was changed by this work.
