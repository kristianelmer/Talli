# #146 full Spec review at 86951dbb

Fixed point `20999f167084492bb4f9a5a50d0cbcd3a48333b3`; reviewed `86951dbb5d14c1b74ca8e1c1b581327bed4b48d2` (five commits, nonempty 95-file diff). **Changes required: one P1, two P2 findings; one P2 subsequently closed.** Exact hashes and reproduction scope: `final-spec-review-86951-bindings.json` beside this report.

- **P1 — Preview outage discards retry identity.** `apps/web/app/actions.ts:3936` redirects preview failures through `failTo`, dropping `taxSettlementOperationId`; the wizard submits `returnTo=/actions` and generates a new UUID without that query value (`TaxSettlementWizard.tsx:25,52`). After a committed-but-lost capture, a retry whose submit-time preview fails therefore loses its original identity and can later duplicate the settlement. The plan requires “Repeated/ambiguous operations retain identity and never post twice.” Exact extracted action/helper execution at follow-up01d600fc reproduced `/actions?error=…` with no operation ID; no new capture or audit ran. Preserve the ID and retry route on preview failure; test committed-response loss → preview outage → same-ID recovery.
- **P2 — Nonfinite text loses characterized validation.** Wizard text such as `abc`, `Infinity` or `1,5` passes through `Number(amount)` (`TaxSettlementWizard.tsx:37`), serializes to JSON null, and strict preview input (`main.py:1819`) returns `REQUEST_VALIDATION_FAILED`. The original returned `invalid_amount` and its Norwegian message. Actual pinned FastAPI reproduction confirms all three. Preserve the backend-owned validation result across this serialization boundary. Spec: “Original coded Norwegian validation messages and API/RLS failures, ordering/defaults.”
- **P2, CLOSED at01d600fc5573967d50879e216c96d96445fdb949 — Submission domain errors lost their code prefix.** The same preview formatter served wizard and submission. The separate submission formatter now restores all seven frozen coded errors; **13 independent pinned transport tests pass**. Screenshot changes are opt-in evidence capture only.

The prior index finding is closed. SQL retains phase/transaction locks, reconciliation/quarantine and both rollback modes; tests add linked effects, late-failure rollback, concurrency, retention and AdminCost replay. The released capture OpenAPI path and every transitive schema are byte-equivalent structurally. Archive retains thirteen fields; exactly two future reads retire, with bounded composition hashes and sibling guards. Annual-estimate policy remains unchanged.

Independently ran **74 pinned backend tests: passed**. Verified checkpoint log hashes; did not rerun DB/browser. Full gates, exact deployment-order evidence and protected integration remain pending, separate from these defects; no #152 stage-exit or production claim.
