# #146 exit-envelope gap closure: aad6bb90

**PASS — the bounded deployment/retention proof gap is closed. Remaining acceptance work is the two linked complete immutable gates, protected merge/exact-main evidence, and their final committed receipt/metadata.** This supplements the unchanged initial `final-spec-exit-envelope-aad6bb90` mapping; its previously open deployment gap is superseded by this receipt.

Independently read the parent’s refreshed script/results and verified all **157** old-backend file hashes against both commit `20999f167084492bb4f9a5a50d0cbcd3a48333b3` and the private pinned source tree. The result binds all three exercised migration artifacts to `aad6bb90951120b09771a23f58d5b662713e7a5a`, including corrected cutover digest `28842782cf47aea849ba25e8dc672c98f0453852ba8cf157e5a58f725209c206`. Manifest output/log hashes match actual bytes; this receipt additionally binds both executable probe scripts.

The probe removes the ambient direct Tax helper grant and four borrowed owner memberships, verifies helper EXECUTE is absent, then applies current cutover and verifies that it establishes the intended grant. After full rollback, the pinned old application uses real `SupabaseLedgerSession` with the restricted backend connection. Results show original receipt201/replayed, fresh linked payment201/not-replayed, and repeat201/replayed with the same returned entry ID. The actual Documents retention owner has helper execution while all four borrowed-owner USAGE checks are false; removal must raise `documents_evidence_linked`. The probe finally applies recutover/contract and verifies the contracted phase.

This directly addresses the earlier privilege-masking uncertainty. Existing permanent lifecycle/concurrency/browser evidence supplies broader row-count, single-writer and ambiguous-retry coverage; this narrow probe does not independently recount every journal/audit effect. Unchanged transport/runtime sources preserve applicability of the prior new-preview-on-old-backend failure proof.

Limits: read-only artifact review, not an independent database rerun. The parent executed a synthetic retained disposable database with authentication substituted by a verified fixture actor; it is not real-company, hosted/provider, whole-old-UI or production evidence. The running full gate is not inspected or credited. No full Company Tax stage10/#152 exit is accepted.
