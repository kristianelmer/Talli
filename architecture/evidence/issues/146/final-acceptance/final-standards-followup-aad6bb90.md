# #146 bounded Standards follow-up

**No actionable Standards findings** in `git diff 096dab57a8593f6df16a2b3e82a382e3d95a9b4f...aad6bb90951120b09771a23f58d5b662713e7a5a` (one commit, 21 files). Reviewed committed objects against the established AGENTS/CONTEXT, ADR0011/0012/0013, module standards and twelve heuristic smell baseline. Exact hashes/assertions: `final-standards-followup-aad6bb90-bindings.json`.

**Narrow grant authority:** `20260913172000_company_tax_settlement_cutover.sql:8,185` adds the helper’s existing identity-guard owner to the migration caller’s temporary memberships and matching cleanup revoke. The membership surrounds the unchanged grant at `:65–67`, which addresses the actual owner of `documents.has_evidence_references_v1`. Removing the two added role tokens recreates the entire base cutover SQL byte-for-byte. Function bodies, fixed search paths, row policies, ordinary application grants and writer boundaries are unchanged. Membership cleanup still executes before commit, and transaction rollback covers errors. This preserves ADR0011’s least-privilege authorization seam rather than relying on ambient fixture authority.

**Meaningful regression test:** `test_company_tax_filing_database.py:235–250` revokes the three owners borrowed for fixture insertion before each cutover/rollback/recutover branch. It then executes the real Documents removal function under its executor and specifically requires `documents_evidence_linked`; a permission error cannot satisfy the assertion. This tests retention behavior, not merely a grant statement.

**Evidence:** the failed-gate transcript confirms two permission failures among 22 cases. Separate retained-database probe logs report all three phases red after removing ambient direct helper execution, then green; the lifecycle log reports 22 passes. Their manifest explicitly withholds fresh-gate/exit credit. All 15 adopted artifact hashes, the failed-gate transcript hash and catalog/source-retirement SQL hashes verify. Five prior Standards artifacts match their private originals exactly.

Independent work here was read-only source/log review and static assertions, recorded in `final-standards-followup-aad6bb90-static.log`; no database/browser/provider/shared-process operations were run. Frozen compatibility authority, mandatory gate commands and criteria/stage state remain unchanged. Fresh immutable gates and protected integration remain pending; no complete-gate or exit certification is implied.
