# Bounded Spec review: 8ba10ae6

**PASS — no actionable Spec finding in this diff.** Reviewed `4ce5f625c95ea14bb188c4169814130b3327e2ff...8ba10ae60a5e45c01a082a93f0b1b73f9943103b` (one commit, eight files). Source hashes and executable evidence are recorded in `final-spec-followup-8ba10ae6-bindings.json`.

The plan requires “Permanent canonical tests retain assertions” and a “complete hydrated wizard/workspace journey.” `package.json:44` and `scripts/test-supabase-local.sh:152` retain predecessor fixture/process/onboarding checks and require the annual journey after Tax lifecycle/contract. Its added contracted-state assertion fails before fixture creation on the wrong topology. The shell failure matrix exercises the annual lane’s nonzero exit and restoration; no optional lane replaces it.

`tests/browser_owner_annual_loop.mjs:800` removes retired opening mirrors and Ledger `setup_id` while preserving canonical RF opening/shareholder facts, Ledger bank input, opening journal lines and opaque provenance. Byte comparison confirms the browser body is unchanged except the topology precondition, all functions after seed are unchanged, and the seed’s remaining historical RF preview/permission data is unchanged. The removed equality assertions compared retired mirror tables; canonical fixture assertions replace those checks.

`tests/support/browser-owner-cleanup.mjs:177` accepts only declared physical tables/partitions, skips retired views, requires replacement Tax/Governance families and deletes by bound company ID. Canonical dependents precede their parents, Banking and Ledger. `tests/support/rf1086-fixture-access.mjs:153` retains loopback/admin/owner validation, internal FK triggers, FORCE RLS and exact prior ACL/role/USER-trigger restoration; the diff adds only ten explicit Governance relations and their owner. No product runtime, migration, criterion or frozen inventory changes occur.

Independent pinned Node **v24.20.0** validation from an outside-repository fixed-source archive: **22 fixture/process tests passed**, **one real Supabase cleanup test explicitly skipped**; **14 mandatory-order/shell-failure tests passed**, including annual-lane failure. Shell commands use test shims; this is not database evidence.

Limits: no independent database/browser rerun or acceptance of the parent’s historical browser/cleanup report. Fresh aggregate, immutable gates and protected integration remain pending. This report gives no full-stage or #152 exit credit.
