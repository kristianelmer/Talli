# #146 capture checkpoint — Standards review

Pinned base `341838edae9747d6794558db1acc92157c51bc67` → candidate `86ae2c5a12661fe79a7a3f93ab47398081c92bac`; nonempty three-dot diff, one commit (“Move atomic tax settlement capture into Company Tax (#146)”), 29 changed files. Exact candidate file hashes: `capture-standards-source-bindings.json`. Reviewed committed objects, not subsequent working-tree changes.

Standards: root AGENTS/CONTEXT, docs/agents/domain.md, ADR0011/0012/0013, affected module/system manifests and documentation. All twelve code-review smell heuristics considered; documented standards take precedence. No formatting findings.

1. **[P2] Documented inventory omission.** `architecture/backend-system.json:501` and `architecture/BACKEND-SYSTEM.md:553` declare only the Tax expansion as a technical migration. The new `supabase/contract-migrations/20260913172000_company_tax_settlement_cutover.sql:62–75` also writes system-owned inventory/source evidence, then reconciliation and phase state. Add this cutover path to both inventories. ADR0013:10–13 requires manifest reconciliation with migrations/documentation; ADR0011:40–49 makes migration infrastructure a declared system responsibility. This matches the existing RF expand/cutover/contract convention.

2. **[P2, heuristic] Possible Refused Bequest.** `apps/backend/src/talli_backend/adapters/postgres_company_tax_filing.py:109` inherits the entire Ledger workflow/session implementation to reuse connection helpers. This includes unrelated Ledger commands and `SupabaseLedgerSession.transaction()` (`supabase_ledger.py:716–738`), which opens another connection as `ledger_workflow_executor`. Replace full business-adapter inheritance with narrow connection-bound infrastructure helpers/composition. The current Tax workflow uses its bound connection correctly; this is a maintainability/authority-boundary hazard, not an observed transaction escape or authorization exploit.

The shared receipt ownership repair preserves helper bodies and full-request fingerprinting while giving the existing actor-RLS owner the necessary authority. Reviewed Tax/Ledger/Banking/Documents paths retain distinct ownership, verified actor checks and restricted execution. No direct ordinary-role bypass was demonstrated.

Validation limit: source review only; parent-reported 76 focused and nine SQL/HTTP tests were not rerun here. At this candidate, all real SQL/HTTP requests have `bankTransactionId=None` (`test_company_tax_filing_database.py:88–92`); the document-failure test at 127–136 fails before posting. Real linked-bank claims and late-failure rollback therefore remain unproven by those nine cases. Web/preview/Archive final contract and complete release/exit gates remain pending; no exit approval is implied.
