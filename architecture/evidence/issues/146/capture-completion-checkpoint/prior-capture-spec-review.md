# Bounded #146 capture Spec review

Reviewed `341838edae9747d6794558db1acc92157c51bc67...86ae2c5a12661fe79a7a3f93ab47398081c92bac`, one nonempty 29-file commit. **One P2 finding; no exit acceptance.** Source SHA256 bindings accompany this report in `capture-spec-review-bindings.json`.

**P2 — Preserve standalone indexes across cutover/rollback.** `supabase/contract-migrations/20260913172000_company_tax_settlement_cutover.sql:50` inventories columns, constraints, policies, triggers and grants but omits standalone indexes; line97 drops the predecessor table. `supabase/rollback/20260913171000_company_tax_settlement_expand.sql:58` rebuilds that table and its constraints, without recreating indexes. Consequently the existing `holding_actions_company_id_year_idx` and `holding_actions_ledger_entry_id_idx` (entry migration `0001_authenticated_workspace.sql:526–527`) disappear after full rollback. The canonical table likewise supplies only a primary key. Company/year archive reads lose their established access path. This leaves the plan's “Both rollback forms … restore one writer at the matching application/schema phase” and ADR0013's rollback/recutover preservation incomplete at the physical-schema level. Include standalone index definitions in the hash-bound inventory, recreate them during rollback, and retain equivalent canonical indexes. Decisive test: compare `pg_indexes` definitions before cutover and after rollback, with canonical company/year and entry-reference indexes asserted separately.

The application preserves receipt replay before fresh validation, existing-action replay, Ledger-owned account mapping and one connection/transaction for Tax, Ledger, Banking and Documents. Phase row locks serialize cutover/rollback with the canonical transaction; source locks and count/digest reconciliation protect migration. No shared receipt helper body or fingerprint serialization changed. The HTTP path and v1 result/error mapping remain intact.

Independently executed **50 pure/application/HTTP tests, all passed in 5.94s**, from an outside-repository archive of the reviewed commit. I inspected the nine committed DB cases but did not rerun them. Their coverage does not yet prove linked-bank capture, failure after posting/bank claim, concurrent sessions, an actual predecessor request after rollback, or the remaining AdminCost caller after shared-helper ownership repair. Root acknowledged these proof gaps and is extending evidence; no unverified permission failure is reported as a defect.

All original six criteria remain pending. Additive preview, web cutover, final Archive contract, full migration/deployment evidence and immutable release gates remain explicit later work. No database, browser, GitHub or repository mutation occurred in this review.
