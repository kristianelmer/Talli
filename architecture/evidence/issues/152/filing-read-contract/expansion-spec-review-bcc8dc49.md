# #152 inert expansion Spec review: bcc8dc49

**PASS for the bounded expansion checkpoint; no actionable defect found.** Reviewed fixed `af95066f0655733b8ea76ef1490b3178864aced7...bcc8dc490a451e9afc181fd9c061afe3da06d120` against #152, #132’s one-writer/reconciliation envelope and ADR0011/13. This is read-only source/evidence review, not an independent database execution.

The migration requires the contracted settlement predecessor, locks all six generic families and captures every source row, including siblings/quarantine. Ownership uses the Tax filing/obligation and linked preview; Tax submissions also verify linked test-run ownership. Conflicting parent filing/company/year, unknown preview obligation and Tax-prefixed Accounts overrides are quarantined. Comments correctly derive their owner from the parent rather than the historically shared `rf1086_preview` label. No legacy mutation or writer replacement occurs.

The six Tax targets retain columns, checks/defaults/indexes and values; subsequent FK reconstruction rewires intra-Tax references while retaining external company/actor/opening-snapshot references. Ordered JSONB count/hash equality is enforced before commit. Business triggers are not copied, so backfill does not generate Audit or Archive-generation events. FORCE RLS, no business policies and explicit grant revocation seal the targets; only private migration-evidence tables receive the store-owner policy. Borrowed memberships, schema CREATE and opening REFERENCES permissions have matching restoration paths within the transaction.

The migration SHA-256 is `ce0a4b9ab6fc7f90ea4cd851e9a2cff2da113db9c461dc5911bd6c7abfc96281`. All committed artifact hashes agree. The saved 33-check seeded probe actually checks exact six-family copies, sibling preservation, three quarantines, count/hash reconciliation, denied target reads and unchanged memberships/schema/opening ACLs, legacy RPC, Audit and generations. Its final transaction rollback is verified. This supports the declared synthetic PostgreSQL scope; it does not test a durable migration commit, HTTP JWT/MFA, a full role-topology matrix, or migration rollback/recutover artifacts.

Technical tables and six new business tables have declared owners; original criteria remain unchanged and pending. Production-history absence is not attested by this six-family snapshot. Refresh after intervening legacy writes, cutover, contracts, both rollback forms, adversarial lifecycle/runtime/browser checks and complete exit evidence remain future work. None is falsely claimed complete here. No repository, database, browser, provider or shared-process mutation occurred.
