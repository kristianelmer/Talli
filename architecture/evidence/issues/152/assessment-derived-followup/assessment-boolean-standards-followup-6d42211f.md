# Standards follow-up — account discriminator and Boolean parity

**Prior account finding closed; one residual P2 Boolean-output finding.** Reviewed `497b91d8643b7639ff1157b66761fc8c2590b850...6d42211fab713a73532c349aa6e7f1e60d625a97`, 17 files. Uncommitted web callers are excluded.

The unchanged account probe now rejects missing/null accounts and empty lines with 422; its valid `7770` control still reports the 100 debit. The shared ECMAScript conversion correctly keeps empty arrays/objects truthy for readiness, payload feedback and preparation blocking. The 45-case Node24 capture reproduces byte-for-byte, and **395 selected backend/characterization/authority-payload tests passed independently**. Nine committed artifact hashes match. The broader recorded 463-test grouping was not rerun in full.

**P2 — Convert the derived Boolean consistently** (`apps/backend/src/talli_backend/modules/company_tax_filing/calculation.py:205`). `build()` still writes `derived.noActivity` using Python `bool`. With `no_activity_confirmed: []` or `{}`, the exact predecessor `buildCompanyTaxReturnPayload` returns `noActivity: true`, while owned `build_company_tax_return` returns `false`. The new fixture compares readiness/feedback only, so its 45 passing cases miss this published payload difference. Use the same `truthy` conversion here and include `derived.noActivity` in the captured assertions. This is a concrete remaining characterization/equivalence defect under ADR0013, not a heuristic smell judgment. HTTP already rejects these malformed Boolean inputs; direct pure/CLI compatibility is the affected boundary.

No further ownership/authority regression was identified. Pure numeric coercion functions and transaction boundaries remain unchanged. Verification used isolated source, fake HTTP sessions and exact legacy-source probes; no database, browser, provider, cutover, full-stage or release-gate acceptance is claimed. Source and transcript hashes accompany this report.
