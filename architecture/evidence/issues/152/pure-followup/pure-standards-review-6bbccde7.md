Two actionable parity findings in fixed `054740b81692e0e59b99a76fff085f322f1968b4...6bbccde76e4d7b47e0c6b2c83f21e46470118d3f`. Both concern ADR0013’s characterization/equivalence requirement and the module’s declared preservation of strict receipt projection/calculation behavior, not Fowler smell judgments.

- **[P2] Preserve WHATWG hostname canonicalization.** `evidence.py:65–83` rebuilds URLs using `urlsplit`/IDNA, which accepts noncanonical numeric hosts the predecessor rejects. With the valid frozen synthetic input, `https://127.1/`, `https://2130706433/` and `https://0x7f000001/` all produce Python projections; unchanged TypeScript rejects each with the canonical-URL error. Canonical `https://127.0.0.1/` succeeds in both. Capture the relevant predecessor numeric/encoded-host boundaries and preserve their acceptance/error behavior before routing this public contract. This is validation drift; the probe performs no URL fetch.
- **[P2] Match JavaScript numeric coercion before calculating amounts.** `numbers.py:8–23` delegates decimal strings to Python `float`, which accepts numeric separators absent from JavaScript `Number`; the hexadecimal branch also misses `OverflowError`. Through `build_company_tax_return`, setting the captured dividend’s `gross_amount` to `"1_000"` changes `derived.dividendIncome` from predecessor zero to Python 1000. A hexadecimal string containing 300 `f` digits yields predecessor zero after finite filtering but raises Python `OverflowError`. Add frozen malformed/overflow input cases and implement the released coercion rules instead of silently accepting Python-only syntax or leaking this exception.

The declared public facts are copied/frozen; calculations/projections perform no I/O or cross-owner reads. Manifest exports and descriptions match the implemented pure contracts. The 18 SQL/RLS capture claims explicitly use synthetic SQL claims and do not certify HTTP authentication, real MFA or UI behavior. Their and the additional boundary fixture’s declared hashes match. Summary collation, routing, schema cutover and full exit remain expressly pending.

Independent isolated validation: 116 new parity/immutability cases plus 53 settlement policy/workflow/API regressions passed (169 total). Additional exact predecessor/current probes demonstrate both findings above with passing controls. All source reads were pinned; no shared repository/database/browser/process mutation occurred. API regression tests use their existing in-process fakes. No complete gate or #152 exit credit is granted.

Companion JSON binds source and private test/probe artifacts. The preserved probe scripts run from the recorded isolated extraction; Node v24.20.0 supplies predecessor execution and Python 3.12 supplies candidate execution.
