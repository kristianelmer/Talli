# #152 TT02 import Spec review: b400103d

**Two actionable findings in the bounded milestone.** Fixed diff: `4c5404dfcedfbd5b858d76e1a7095ec2bd800247...b400103d2d7204c9a2154fd953a03ccb00041ad4`. #152 requires **“Supported company-tax cases produce outputs identical to current schema and TT02 evidence.”** The milestone also expressly preserves legacy validation and sanitized failure behavior.

**SPEC-152-IMPORT-1 (P2): accepted deep JSON becomes HTTP 500.** `main.py:9836–9839` constructs the immutable command outside its JSON recursion guard; `public.py:394–395` recursively freezes every field, including ignored data. A valid captured evidence document plus an ignored 600-level array is only 3,864 bytes. Pinned Node 24/predecessor `054740b8` accepts it and produces the exact captured projection; the new actual FastAPI route returns 500 with no transaction opened. Preserve valid bounded evidence through iterative conversion, including ignored nested data, and handle unsupported input shapes as domain errors. The private unchanged-fixture regression fails as expected.

**SPEC-152-IMPORT-2 (P2): persistence errors lose their legacy presentation.** `postgres_company_tax_filing.py:55–56` maps SQL invalid-payload, forbidden-content and replay-conflict failures to projection-invalid/422. `transport.ts:56–60` consequently presents “Ugyldig TT02-evidens”. The original action (`054740b8`, `actions.ts:5051–5055`) presents “TT02-evidensen kunne ikke lagres.” for these persistence failures, retaining a separate MFA message. Actual API tests with the committed mapper and extracted unchanged presenter reproduce all three differences. Separate projection-invalid from persistence-rejected errors. UI is still on the legacy path; this is an exported-contract defect, not an activated UI regression.

Seventeen committed application/API/adapter tests pass independently. Static SQL-body comparison preserves the predecessor validation, hash, locking, replay and attribution logic, with explicit owned-table/Company Access substitutions, phase gating, and Audit removal into the same application transaction. Audit runs only for creation; a failure aborts the transaction. The shell leaves `public.audit_events` canonical under #132’s unmigrated-dependency allowance and grants no #155 completion.

All nine artifact hashes, three migration hashes and legacy SQL-body binding match. The recorded 19 SQL cases demonstrate rollback-only synthetic authorization, replay and injected Audit failure; I did not execute SQL or prove concurrent sessions/HTTP JWT/MFA. OpenAPI adds two schemas/one route only. Original requirements remain unchanged and pending. No provider, cutover, owner UI, rollback-rehearsal or full-stage acceptance claim.
