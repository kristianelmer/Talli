Bounded browser slice PASS; overall #152 has two actionable cleanup findings.

Reviewed `5a0634ac5db27f63ab2e05e437f24712c282fd7a...9f5c5a5b15971de69327c3e242a603c32c7c4b20`, plus remaining obligations against stage baseline `054740b81692e0e59b99a76fff085f322f1968b4`. No application, generated-client or SQL implementation changes occur in the browser commit. The extracted MFA helper is byte-identical. All eight source and two evidence manifest hashes match.

The recorded two real local browser journeys support the stated AAL1 denial, wrong-year rejection, committed-response-loss replay identity, one import/Audit, owned permission/manual evidence, current-owner revocation denial and positively complete scoped history. Synthetic TT02 feedback stays pending; production remains blocked. I inspected the recorded pass and producer, without rerunning browsers or databases.

P2 — Retired RPC remains mandatory. `scripts/assert-deployed-schema-contract.mjs:13` and `tests/deployed_schema_contract.test.mjs:22` still require `/rpc/import_company_tax_tt02_evidence`, although the Tax filing contract drops it. The pure probe confirms a correctly contracted schema is rejected and a restored obsolete endpoint is not forbidden. #132 requires “generic filing storage cannot remain authoritative” and removal of capability compatibility debt. Update the Tax deployment expectation and exercise correct absence/restored-endpoint rejection, accounting explicitly for any supported pre-contract phase.

P2 — Archive completeness guard omits migrated sources. `tests/company_access_cancellation_schema.test.mjs:98–199` and `architecture/company-archive-sources.json` omit the five Tax filing families. The named complete-inventory test still passes when all five Tax filing generation triggers are removed in a private SQL copy. This violates #132’s “manifests, documentation, and Graphify agree” assurance. Add those canonical families, derive their scopes from the filing cutover, and check `loadPresentedCompanyTaxSource` lies between begin/complete. Current runtime triggers and read ordering are present; no runtime invalidation defect is demonstrated.

Separate remaining release proof: no Tax-filing-specific deployment-order protocol rehearsal was found; #132 explicitly requires “deployment-order compatibility.” SQL rollback tests and prior settlement proof do not cover the newly introduced filing routes. Refresh obsolete intermediate MODULE prose before exit. Declared full gates/integration/registry adoption remain pending, without being counted as defects. No full-stage or successor acceptance.
