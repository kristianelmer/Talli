Spec review: assessment previews at e2dfdf54, follow-up 497b91d8

Reviewed `git diff 909bd28d...e2dfdf54` and the separate committed `e2dfdf54...497b91d8` correction. One concrete finding was identified and is now closed at the HTTP boundary.

SPEC-152-ASSESSMENT-1 (P2, closed): `apps/backend/src/talli_backend/main.py:1048–1055` accepted nested malformed facts through `dict[str, Any]`. Actual authenticated requests with admin-cost `lines: {}` or `lines: ""` returned HTTP 200 and a zero estimate; unchanged predecessor TypeScript rejects both. An array-valued `shareholder_loans` answer returned clear readiness while predecessor feedback emits the loan-review block. This contradicted MODULE.md’s “malformed facts fail validation” and #152’s deterministic fail-closed/parity requirements (GH-152-A1/A2). Three independent probes failed on e2dfdf54. At 497b91d8, consumed container/Boolean validation rejects these requests; all three original probes pass alongside 182 committed readiness tests (185 total). This closes HTTP exposure only; direct pure/CLI malformed-input truthiness remains separate work acknowledged by the implementer.

The 56-case capture independently reproduced byte-for-byte using pinned Node 24.20.0 and predecessor source bytes. The original snapshot’s 167 readiness/pure/API tests passed independently. These establish the captured ordering, company/year holding checks, cross-company/all-year estimate aggregation, immutable facts, and authentication before calculation. Static review found no additional actionable defect in the bounded slice: the routes perform no business persistence or membership/completeness attestation, and generated guards check response shape, finite amounts and readiness scope.

All 11 initial manifest artifacts and the follow-up log match their committed hashes. Parsed OpenAPI adds exactly two paths and five schemas without changing existing entries. Requirements remain unchanged. Web retirement, source-backed handoff, durable authorization/history, cutover, full immutable gates and protected integration are excluded and remain pending; there is no full-stage, provider, hosted or successor acceptance.
