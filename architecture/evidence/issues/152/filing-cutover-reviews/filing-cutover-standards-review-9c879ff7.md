# #152 filing lifecycle — Standards review

**CHANGES REQUESTED — one P2 permission-restoration defect.**

Pinned scope: `git diff f9838c6096640d9810381d86ef64dd8177ee35ac...9c879ff78b5ec5aa3bb639e5de4a39ab38ef9cfc`; one commit, `feat(company-tax): rehearse reversible filing ownership cutover`. Standards axis under code-review, ADR0011–0013, module/system ownership and the supplied Fowler heuristics; repository rules override heuristic smells.

**P2 — temporary Archive permission becomes permanent.** In `supabase/contract-migrations/20260914012503_company_tax_return_cutover.sql:195–197`, effective `has_function_privilege` is sampled after borrowing the Archive function-owner role with `INHERIT true` (lines 10–17). That membership supplies EXECUTE even when the migration principal had no original direct grant. The unconditional grant to `session_user` then creates a direct ACL entry; restoration at lines 223–228 skips revocation because the sampled value was true. Removing borrowed memberships therefore leaves the new permission. This conflicts with the declared temporary-privilege restoration in `apps/backend/src/talli_backend/modules/company_tax_filing/MODULE.md:37` and ADR0011's least-privilege boundary. Remove the redundant direct grant, or restore the original direct ACL independently of temporary effective privileges. Add an exact Archive-function ACL snapshot and a no-original-direct-grant control: `environment_snapshot` in `apps/backend/tests/test_company_tax_return_lifecycle.py:23` currently omits it. This is a concrete authority defect, not a smell judgment; no database reproduction was performed by this reviewer.

No additional actionable source defect found. All four relocated artifacts are byte-identical to the predecessor. All four new SQL hashes and six lifecycle artifact hashes match the manifest. Named ordering keeps settlement contraction before filing expansion/contracts; catalog discovery adds deferred CREATE TABLE coverage without changing retained-composition guards. Finite families, latest-row reconciliation, static retired-writer fences, shared runtime phase locking and retained Accounts/Archive behavior were inspected.

Independent isolated Node v24.20.0 architecture regression: 2/2 PASS, including mutation matrices and the new uncatalogued deferred-table negative. The six SQL scenarios and eleven race checks are inspected committed evidence only; no shared checkout, database, browser, or provider operations and no full-gate or stage-exit credit.
