**PASS — no actionable Standards finding** in `ac9b349ab58b89bdd0efa4810ef382844be5c6fa...849f81d4cb8a9405504138928539f8026d665852`.

The annual fixture now requires the contracted filing phase and seeds the same synthetic Tax permission through its canonical owner. It positively checks that the retired public Tax permission mirror is absent. The public Accounts permission and historical RF facts remain unchanged; no application, migration, writer fence or production activation changes accompany this test correction. This conforms to ADR0011 ownership and ADR0013 canonical-writer retirement.

Cleanup adds exactly six literal Tax families and deletes dependent submissions/comments/overrides before previews/evidence, and all Tax rows before their company. The existing local-host/company restriction, physical-relation discovery, transactional owner borrowing, internal foreign-key enforcement and grant/trigger restoration implementation is unchanged. New model assertions check Tax presence only at the contracted topology and deletion ordering. No material heuristic smell warrants a finding.

Independent pinned Node 24.20.0 safety/process tests: **22 passed, one existing optional live-database test skipped**. Initial missing isolated dependency failure and final pass are both retained. Byte comparison confirms the annual test’s complete pre-seed body, including browser assertions, is unchanged except for the added filing-phase precondition. The fixture authority helper is byte-identical to the baseline.

All thirteen manifest artifact hashes match. Adopted whole-stage Standards report and JSON match the private originals exactly. Retained red evidence shows the legacy writer fence rejected the old fixture; focused green and remaining browser transcripts support the bounded local claims. I did not rerun browser/database journeys. The failed ac9b gate correctly receives zero complete-gate credit, and the follow-up does not certify the running gate, full-stage exit, protected integration or successor work. No shared checkout or runtime was mutated.
