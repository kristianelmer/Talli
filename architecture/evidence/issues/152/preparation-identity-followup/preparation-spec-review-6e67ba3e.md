# #152 preparation Spec review: 6e67ba3e

**One actionable transaction-boundary finding.** Fixed diff: `5d240525284e6aa92e2875940a638adb110421a6...6e67ba3e0759641db845449756306df444a3a9b1`. Review covers only the declared existing Tax controls; later uncommitted UI work is excluded.

**SPEC-152-PREP-1 (P2): reject acknowledgement identity corruption before commit.** `company_tax_filing_workflow.py:174–175` returns the repository result and closes its transaction before `main.py:9890–9891` checks the returned record ID. An injected different valid UUID produces actual HTTP 503 (`COMPANY_TAX_DEPENDENCY_UNAVAILABLE`), but the observed sequence is **begin → acknowledgement-write → commit**. The declared source rule says **“Missing source is unavailable”**; ADR0011 assigns mutations to named application workflows **“inside one short Postgres transaction.”** The identity check belongs inside that transaction so a rejected result rolls back. Keep the HTTP check as additional defense. The independent regression expecting rollback fails. This demonstrates malformed-result handling with a persistence double, not that the current SQL returns a wrong ID or that production data changed.

All 20 committed legacy normalization/acknowledgement captures reproduce byte-for-byte using Node 24.20.0 and the three source files independently hashed against pinned `054740b8`. The 24 committed preparation tests pass independently in 1.99s. The reproduced captures preserve trim behavior, risk/status validation, optional-null normalization and fixed-clock projections; they do not prove every input combination.

Static SQL review confirms phase gating, accepted owner/reviewer decisions, hard-block acknowledgement rejection, fresh MFA for permission/manual evidence, source-derived preview scope, attribution, and permission upsert identity. Generic Tax/Accounts writers and web Audit continuations remain unchanged. Company-wide permission/evidence facts do not assert year-specific production completeness.

All twelve adopted artifact hashes and four SQL source bindings match. The saved 60-check probe includes 45 prior checks and 15 new preparation checks, using synthetic claims and a rollback-only phase change. I did not execute SQL or verify real JWT/MFA/concurrency. OpenAPI adds exactly five schemas and six routes, with no existing schema/path changes. The six original criteria remain unchanged and pending. No provider, preview-generation, durable cutover/rollback or full-stage acceptance credit is granted.
