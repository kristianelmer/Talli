# #152 CLI payload Spec review: c3eb992c

**Two parity findings.** Reviewed `8b3e0cb340d962c8140b1ac72ceada59d4d4aea7...c3eb992c30531f7402a31ceb830164fa4fda1654`. Both violate #132’s characterization requirement to preserve “results, coded errors, ordering, defaults” and #152-A1’s unchanged supported statutory outputs.

1. **Explicit null document reference loses its rejection** — `authority_tools/company_tax_payload.py:24`. The mapping’s `value.get('currentDocumentReference')` collapses JSON null and omission into the public contract’s absent marker. Both `company_tax_envelope` and `company_tax_validation_envelope` previously reject an explicitly null reference with `Local authority payload generation failed.`; the new CLI returns an envelope without that reference. Missing, empty and valid-string controls retain their former results. Preserve field presence and route explicit null through the existing required-reference rejection.

2. **A valid integral JSON number changes XML dates** — `authority_tools/company_tax_payload.py:13` passes `2025.0` through to `calculation.py` date interpolation. Exact old CLI output is `<dato>2025-01-01</dato>` / `2025-12-31`; new output contains `2025.0-01-01` / `2025.0-12-31`. JSON has one numeric type, and the old Node boundary normalizes this integral number. Preserve the predecessor numeric string rendering before generating statutory date text.

Independent tests pass **143 authority cases** with mocked providers and **both Node dispatch tests** against privately extracted committed sources. Exact predecessor `_filing.payload` subprocess probes on Node v24.20.0 versus current in-process payload produce **3 failing comparisons, 7 passing controls**. Inputs, old/new complete outputs and red regression log are privately retained and SHA-bound in the companion JSON. An initial isolated Node test failed because the private extraction omitted `apps/web/package.json`; after copying those exact committed module settings, both pass. No product change was needed for that fixture correction.

No other scoped defect found: four Tax branches are removed from the Node dispatcher, Annual Accounts remains unchanged, the public preparation gate rejects blocking feedback before rendering, and input/output size/nonstandard-JSON checks preserve the intended local boundary. Six issue criteria remain pending; imported prior review bytes and artifact hashes match. This milestone supplies no API/web/persistence cutover, real-provider validation or full-stage exit credit. No shared repository, database, browser, configuration or provider mutation occurred.
