One actionable Standards finding in `8b3e0cb340d962c8140b1ac72ceada59d4d4aea7...c3eb992c30531f7402a31ceb830164fa4fda1654`.

**[P2] Preserve explicit-null document-reference rejection.** `apps/backend/src/talli_backend/authority_tools/company_tax_payload.py:20–26` maps `value.get('currentDocumentReference')` to the public envelope’s optional field. An explicit JSON null becomes indistinguishable from an omitted key; the public mapper omits it, bypassing the predecessor’s required-reference rejection. Exact pinned dispatch probes demonstrate this for both `company_tax_envelope` and `company_tax_validation_envelope`: predecessor exits with its redacted failure, while candidate emits reference-free XML. Omitted references still succeed, empty strings still reject and valid references still succeed in both implementations. Preserve key presence at the transport mapping boundary and add both null cases without changing the existing omission behavior. This is an ADR0013 behavior-equivalence/validation regression, not a heuristic smell.

The capability owns the blocking-feedback decision via `prepare_company_tax_return`; the CLI adapter only maps fixed operations to declared public immutable contracts. Error wrapping remains generic and unreflective, input/output limits remain eight MiB, and nonstandard JSON constants are rejected before dispatch. No provider, credential, endpoint or storage policy moves into the pure capability. New public exports match the manifest and documentation.

Annual Accounts preservation is independently byte-checked: removing only the new Tax branch reproduces the predecessor `_filing.py` exactly; its Node dispatcher’s Annual Accounts case and stdin/error/size-limit loop are byte-identical. The four retired Tax Node branches now reject explicitly. No API/web/SQL cutover or full-stage exit is claimed.

Independent isolated validation: 81 focused Python authority/CLI tests and both Node dispatch tests passed; the eight-case predecessor/current probe finds the null gap despite those passes. All six adopted artifact hashes match committed bytes. The parent’s broader 143-test mocked-provider result is recorded evidence, not an independent 143-test rerun here. Used pinned Node v24.20.0 and the existing standalone Python environment against a temporary extraction. No shared repository/configuration, database, browser or provider mutation occurred. The companion JSON binds sources, probe and transcripts.
