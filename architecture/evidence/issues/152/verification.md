# Company Tax filing — #152 verification

Company Tax's filing slice is implemented. The settlement slice remains the completed #146 predecessor. This report will be finalized only after the two new complete immutable gates, independent exit review, protected PR integration, exact-main CI/Preview checks and published receipt reread pass. #153 remains unclaimed.

## Original criteria and evidence

1. **Supported outputs:** `legacy-characterization.json`, numeric/Boolean/import-boundary captures, preparation and authority-summary evidence bind the unchanged predecessor to Python payload, XML, evidence and error results. The official XSD source remains tag v1.62.47 / commit 7ac8c6a32238dd0d53e7ac01a6949bb3376f2bba. Archived genuine TT02 evidence is preserved byte-for-byte; synthetic positive import fixtures are explicitly separate.
2. **Fail-closed statutory gates:** The original 2025 support, participation exemption, no-attachment boundary, ordered readiness feedback and JavaScript numerical/truth-value behavior remain characterized. Caller-fact HTTP assessments are previews only.
3. **Authorized transitions and replay:** The capability owns the preserved authority polling and local human-confirmed rehearsal. Pure/transport/workflow tests, seven SQL lifecycle scenarios, eleven transaction-race checks, real owner/TOTP browser evidence and the twenty-one-check deployment-order matrix cover identity, current owner checks, Audit atomicity, original receipt IDs, ambiguous-response recovery and both rollback forms. Production transport stays disabled and pending TT02 feedback is never relabelled authority acceptance.
4. **Owner workflow:** The generated client/FastAPI routes own Tax previews/overrides/comments/acknowledgements, permissions/manual evidence and raw TT02 import. Workspace, Annual and Archive consume the owned Tax read projection; web Tax calculations and the four CLI TypeScript subprocess branches are removed. The original owner-facing entry points remain. The real browser tests cover import, replay, current owner revocation and all settlement kinds.
5. **Single ownership and cleanup:** All six Tax filing families are canonical in company_tax_filing, with exact row and schema reconciliation, original foreign keys and preserved sibling Accounts rows. The retired legacy RPC is absent after contraction, explicit CHECK fences prevent legacy Tax writes, and full rollback restores the latest owned rows and the exact predecessor RPC. The Tax legacy facade and five duplicate TypeScript policy files are removed. The remaining sixteen Annual-owned shared tuples retain their approved scope until the last Accounts source migrates. Archive inventory includes the five newly owned trigger sources and enforces generation-boundary reads.
6. **Full stage exit:** PENDING two new linked complete gates and final protected integration/receipt. No earlier #146 gate is counted as a #152 exit gate. Failed gates 9f5c5a5b and 41bcead1 have zero credit. The Archive VM harness omission and stale two-file Tax database lane expectation were corrected; the mandatory lane retains settlement expansion/runtime and filing lifecycle coverage.

## Source handoff

The immutable company/year/obligation source carries evidence identity/version/digest/time, positively verified recorded-history coverage, original submission modes and actor references, unknown outcomes/incidents and explicit correction links. Empty rows without positive coverage cannot mean no history. A complete proof is scoped to Talli-recorded Company Tax, not external authority history. The existing disabled-production gate decisively blocks current readiness; it certifies no other prerequisite. A later activation must extend the versioned source with durable Annual/Ledger/Investments facts. Billing alone owns commercial/refund decisions.

## Deployment and boundaries

The twenty-one-check matrix uses a disposable restored database and a dedicated synthetic JWT realm. Legacy requests traverse real loopback PostgREST; current requests traverse the actual FastAPI ASGI boundary and restricted PostgreSQL adapter. It checks predecessor, inert expansion, owned cutover, contract, contract-only rollback, full rollback and re-cutover, including fresh receipts in both eras. Full old/new browser deployment is not claimed; real Auth/TOTP is covered by the separate current owner browser. Main retained local topology is contracted and synthetic fixtures are removed. Hosted resources, authority providers and production have not been changed.

Live pre-integration verification at 2026-09-14 03:33:06 UTC confirms talli.no remains on frozen d331ee2717d1eeacef0d81db42b9d4fb5848b408 (dpl_HuHzBsgpcgBVTGsFiFLEEy3PqN5f). Main still requires strict Release gate with enforce_admins and no bypass. Refresh these checks at final integration.
