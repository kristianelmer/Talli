# #152 filing-read Spec review: b1bf9e9f

**One actionable public-boundary finding; no demonstrated HTTP or SQL authorization defect.** Reviewed fixed `bcc8dc490a451e9afc181fd9c061afe3da06d120...b1bf9e9f7b5697226815a603743dc58e2b751e83` against unchanged #152 criteria, source contract, and the #132/ADR0011/13 owner boundary and one-writer requirements.

**SPEC-152-READ-1 (P2): reject malformed empty family containers.** `apps/backend/src/talli_backend/modules/company_tax_filing/public.py:191` freezes collections without requiring an array; `workspace.py:10–15` validates only by iteration. For any of the six families, `{}` or `""` therefore passes as an empty iterable. The returned public projection contains a mapping proxy or string instead of its declared tuple of rows, and the named workflow returns it successfully. The specification says **“Missing source is unavailable”**; this boundary is explicitly described as complete immutable predecessor projections. Require each family to be a list/tuple before freezing and reject malformed containers with `COMPANY_TAX_DEPENDENCY_UNAVAILABLE`. Twelve independent pinned regressions fail (six families × empty object/string). The HTTP wire model rejects these shapes, so this does **not** establish an HTTP false-success or disclosure.

The existing 36 workspace/API tests pass independently from privately extracted committed sources. Their receipt metadata, calls and payload-reference assertions preserve nested JSON. An initial private collection attempt lacked the pinned #146 fixture; copying that exact committed fixture resolved setup without source changes.

The SQL read checks actor identity and accepted membership, conceals outsiders, returns unavailable outside cutover/contracted phases, and adds no writer. All six owner tables retain FORCE RLS. Yearly filtering leaves company-wide permissions/test evidence explicitly distinct from production completeness. The generated client checks response shape, company/year, duplicate IDs and linked references; transport propagates failed reads without fallback.

All nine evidence artifact hashes match. The saved 45-check SQL probe includes the prior 33 expansion checks and 12 read checks; it uses synthetic claims and a temporary phase change in a rollback-only transaction. I did not execute SQL. OpenAPI adds exactly seven schemas and one path with no changes/removals to existing schemas/paths. Original six criteria remain pending. UI/cutover/mutations, positive production completeness, rollback, full gates and protected integration remain outside this checkpoint; no exit credit is granted.
