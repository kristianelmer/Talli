import {readFileSync,writeFileSync} from 'node:fs';
import {createHash} from 'node:crypto';
import {pathToFileURL} from 'node:url';
import {resolve} from 'node:path';
const root=process.cwd();
const lib=(name)=>import(pathToFileURL(resolve(root,'apps/web/app/lib',name)).href);
const {buildCompanyTaxReturnPayload}=await lib('company-tax-return.ts');
const {estimateAnnualTax}=await lib('tax-settlement.ts');
const {renderCompanyTaxReturnXml}=await lib('company-tax-return-xml.ts');
const {renderCompanyTaxReturnEnvelope,renderCompanyTaxReturnValidationEnvelope,summarizeCompanyTaxReturnValidation}=await lib('company-tax-return-authority-payload.ts');
const {buildCompanyTaxReturnEvidencePersistence}=await lib('company-tax-return-submission.ts');
const fixtureSource=readFileSync('tests/company_tax_return_payload.test.mjs','utf8');
const fixturePrefix=fixtureSource.slice(fixtureSource.indexOf('const readyBillingEntitlements'),fixtureSource.indexOf('test("builds'));
const {annualData,ledgerEntries,dividendAction}=new Function(fixturePrefix+';return {annualData,ledgerEntries,dividendAction};')();
const base=()=>structuredClone({companyOrgNumber:'314259521',companyPartyNumber:'9000020078',incomeYear:2025,annualData,ledgerEntries,holdingActions:[dividendAction]});
const capture=(fn)=>{try{return {value:fn()};}catch(e){return {error:{name:e.name,message:e.message}};}};
const cases=[];
const add=(id,mutate)=>{const input=base();mutate?.(input);const payload=capture(()=>buildCompanyTaxReturnPayload(input));const output={payload,annualEstimate:capture(()=>estimateAnnualTax(input))};if(payload.value){output.xml=capture(()=>renderCompanyTaxReturnXml(payload.value.fields));if(output.xml.value){const envelopeInput={...output.xml.value,companyOrgNumber:input.companyOrgNumber,incomeYear:input.incomeYear,createdBy:'Synthetic & Talli <owner>'};output.validationEnvelope=capture(()=>renderCompanyTaxReturnValidationEnvelope(envelopeInput));output.submissionEnvelope=capture(()=>renderCompanyTaxReturnEnvelope({...envelopeInput,currentDocumentReference:'synthetic-current-&-reference'}));}}cases.push({id,input,output});};
add('supported-dividend');
add('default-party-number',x=>delete x.companyPartyNumber);
add('no-activity-zero',x=>{x.annualData.no_activity_confirmed=true;x.ledgerEntries=[];x.holdingActions=[];});
add('missing-annual-data',x=>x.annualData=null);
add('owner-dividend-review',x=>x.annualData.answers.declared_owner_dividends=true);
add('shareholder-loan-answer',x=>x.annualData.answers.shareholder_loans=true);
add('shareholder-loan-entry',x=>x.ledgerEntries[0].entry_type='shareholder_loan');
add('share-sale-answer-warning',x=>x.annualData.answers.bought_or_sold_shares=true);
add('blocking-action',x=>x.holdingActions[0].risk_level='block');
add('unclear-exemption',x=>x.holdingActions[0].payload.tax_treatment='unknown');
add('manual-warning-unaccepted',x=>x.ledgerEntries[0].risk_flags=['synthetic_warning']);
add('manual-warning-accepted',x=>{x.ledgerEntries[0].risk_flags=['synthetic_warning'];x.ledgerEntries[0].warning_accepted_at='2026-01-01T00:00:00Z';});
add('no-activity-contradiction',x=>x.annualData.no_activity_confirmed=true);
add('financial-classification-mismatch',x=>x.holdingActions[0].payload.gross_amount=99999);
add('ledger-only-financial-fallback',x=>x.holdingActions=[]);
add('zero-classified-dividend',x=>x.holdingActions[0].payload.gross_amount=0);
add('admin-cost-entry-estimate',x=>x.ledgerEntries[0].entry_type='admin_cost');
for(const cost of [0,0.005,0.015,1.005,2.675,100000,100001])add('cost-'+String(cost),x=>x.ledgerEntries[0].lines[2].debit=cost);
add('interest-and-split-costs',x=>{x.ledgerEntries[0].lines.push({account:'8050',debit:0,credit:125.5},{account:'6700',debit:990,credit:0});});
for(const result of [20000,-5000,0])add('share-sale-'+String(result),x=>{x.holdingActions.push({...structuredClone(dividendAction),id:'synthetic-sale',action_type:'share_sale',payload:{gain_or_loss:result,tax_treatment:'fritaksmetoden',calculation_id:'synthetic-calculation'}});if(result>0)x.ledgerEntries[0].lines[1].credit+=result;if(result<0)x.ledgerEntries[0].lines.push({account:'8090',debit:-result,credit:0});});
add('split-taxable-sale',x=>{x.holdingActions.push({...structuredClone(dividendAction),id:'synthetic-sale-split',action_type:'share_sale',payload:{book_gain_or_loss:20000,exempt_gain:18000,taxable_gain:2000,non_deductible_loss:0,deductible_loss:0,tax_treatment:'fritaksmetoden',calculation_id:'synthetic'}});x.ledgerEntries[0].lines[1].credit+=20000;});
add('fund-distribution',x=>{x.holdingActions=[{...x.holdingActions[0],action_type:'fund_distribution_received',payload:{dividend_portion:100000,taxable_add_back:3000,tax_treatment:'fritaksmetoden'}}];});
add('investment-and-debt-balances',x=>{x.ledgerEntries[0].lines.push(...['1300','1350','1800','1810','1815'].map((account,i)=>({account,debit:(i+1)*10.25,credit:0})),{account:'2255',debit:0,credit:42.5},{account:'2990',debit:0,credit:100});});
add('escaped-source-identity',x=>x.holdingActions[0].id='synthetic-&<>"\'');
const summaryInputs=['<r><resultatAvValidering>validertOK</resultatAvValidering></r>','<r><resultatAvValidering>validertMedFeil</resultatAvValidering><avvikstype>Z2</avvikstype><avvikstype>A1</avvikstype><avvikstype>A1</avvikstype><veiledningstype>V2</veiledningstype><aarsakTilValidertMedFeil>feil</aarsakTilValidertMedFeil></r>','<x:r><x:resultatAvValidering>other</x:resultatAvValidering><x:avvikstype> Å </x:avvikstype><x:avvikstype>Æ</x:avvikstype><x:avvikstype>Ø</x:avvikstype><x:avvikstype>A</x:avvikstype></x:r>','<r><avvikstype>a\n b</avvikstype><avvikstype>nested<child/></avvikstype></r>','','not XML'];
const summaries=summaryInputs.map(input=>({input,output:capture(()=>summarizeCompanyTaxReturnValidation(input))}));
const evidence=JSON.parse(readFileSync('docs/filing/evidence/company-tax-tt02-2026-07-14.json','utf8'));
const evidenceCases=[];
function imported(id,mutate){const input={companyId:'00000000-0000-0000-0000-000000000152',expectedCompanyOrgNumber:evidence.companyOrgNumber,expectedIncomeYear:2025,evidence:structuredClone(evidence),evidenceUrl:'https://evidence.example/synthetic.json',recordedBy:'00000000-0000-0000-0000-000000000153'};mutate?.(input);evidenceCases.push({id,input,output:capture(()=>buildCompanyTaxReturnEvidencePersistence(input))});}
imported('existing-sanitized-TT02');imported('wrong-company',x=>x.expectedCompanyOrgNumber='314259521');imported('wrong-year',x=>x.expectedIncomeYear=2026);imported('unfinished',x=>x.evidence.status='awaiting_person_confirmation');imported('production-enabled',x=>x.evidence.productionEnabled=true);imported('wrong-scope',x=>x.evidence.scope='wrong');imported('unsafe-url',x=>x.evidenceUrl='data:text/plain,unsafe');imported('blank-url',x=>x.evidenceUrl='');imported('authority-not-accepted',x=>x.evidence.authorityValidation.result='validertMedFeil');imported('missing-receipt',x=>delete x.evidence.receipt);imported('secrets-flag',x=>x.evidence.secretsStored=true);
// Preserve archived TT02 bytes and their current rejection above. These separate
// synthetic cases use the successful fixture already pinned in the source inventory.
const submissionFixtureSource=readFileSync('tests/company_tax_return_submission.test.mjs','utf8');
const submissionFixturePrefix=submissionFixtureSource.slice(submissionFixtureSource.indexOf('const instanceId'),submissionFixtureSource.indexOf('function project('));
const syntheticEvidence=new Function(submissionFixturePrefix+';return companyTaxEvidence();')();
function syntheticImported(id,mutate){imported('synthetic-'+id,x=>{x.evidence=structuredClone(syntheticEvidence);mutate?.(x);});}
syntheticImported('completed-pending-feedback');
syntheticImported('blank-url',x=>x.evidenceUrl='');
syntheticImported('second-owner',x=>x.recordedBy='00000000-0000-0000-0000-000000000154');
syntheticImported('explicit-recorded-time',x=>x.recordedAt='2026-07-14T12:33:00.000Z');
syntheticImported('unsafe-url',x=>x.evidenceUrl='data:text/plain,unsafe');
syntheticImported('wrong-company',x=>x.expectedCompanyOrgNumber='314259521');
syntheticImported('wrong-year',x=>x.expectedIncomeYear=2026);
syntheticImported('unfinished',x=>x.evidence.status='awaiting_person_confirmation');
syntheticImported('production-enabled',x=>x.evidence.productionEnabled=true);
syntheticImported('wrong-scope',x=>x.evidence.scope='wrong');
syntheticImported('authority-not-accepted',x=>x.evidence.authorityValidation.result='validertMedFeil');
syntheticImported('missing-receipt',x=>delete x.evidence.receipt);
syntheticImported('secrets-flag',x=>x.evidence.secretsStored=true);
const data={schemaVersion:'1.0',sourceRevision:'054740b81692e0e59b99a76fff085f322f1968b4',runtime:process.version,submissionFixturePrefixSha256:createHash('sha256').update(submissionFixturePrefix).digest('hex'),caseFixturePrefixSha256:createHash('sha256').update(fixturePrefix).digest('hex'),cases,summaries,evidenceCases};
const json=JSON.stringify(data,null,2)+'\n';writeFileSync('architecture/evidence/issues/152/legacy-characterization.json',json);
console.log(JSON.stringify({payloadCases:cases.length,validationSummaryCases:summaries.length,evidenceCases:evidenceCases.length,sha256:createHash('sha256').update(json).digest('hex')}));
