One P3 evidence correction; no implementation or cleanup defect found in this bounded Standards review.

STD-153-DEPENDENCY-1 — Describe the actual MFA control. `architecture/evidence/issues/153/dependency-integration/verification.md:9` says the new Support test asserts “stale MFA”. However, `apps/backend/tests/test_annual_accounts_filing_lifecycle.py:379` always supplies a current TOTP timestamp; `fresh=False` only changes AAL2 to AAL1. The rejection at lines403–404 proves insufficient assurance, not expiry. Change the claim to AAL1 rejection or add an expired-timestamp AAL2 control. This is an evidence-accuracy correction under ADR0013’s security/RLS verification requirement, not a Fowler smell or a claim that runtime expiry is broken. Parent confirmed a separate correction is planned.

Reviewed immutable 9a3d8d8068749635b64645936f2d7398e0bc11c9...53b3ae3e5e3dcaf020e25113a5a31f9b8ca65701 using the established Standards sources and smell baseline. Only five test files and evidence changed. The Archive VM now supplies the existing owned Accounts dependency, retains the original frozen chain literal, and asserts year filtering, company-wide comments, linked evidence, payload identity and begin/complete ordering. The presenter control verifies explicit safe projection. Backup coverage names all six canonical tables and preserves evidence-before-submission restore order.

Accounts joins the exhaustive, disjoint mandatory backend partition. The actual shell tests retain failure propagation and generated-file restoration while adding Accounts database/browser phases. New SQL tests use explicit executor roles, nonmember Support access and exact scoped fields; the existing outer rollback verifies global membership, schema and fixture restoration. I did not execute SQL.

Independent pinned Node v24.20.0 checks: 33 Archive/backup tests and four focused shell/no-configuration tests passed. All19 manifest hashes and four private review adoptions match exactly. The retained 28-test SQL and 58-test CI passes were inspected, not rerun. Failed gate/probe logs remain preserved. No full-gate, browser, hosted, provider or stage-exit credit is granted.
