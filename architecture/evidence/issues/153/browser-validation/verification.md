# Final filing browser verification

The isolated focused producer retains the ordinary Supabase startup, explicit topology transformations, Ledger authority, Corporate lifecycle, Billing Node contract/rollback/recutover, all Tax/Accounts SQL and activation, final advisors, and four final filing browser lanes. It omits enumerated earlier tests and the already-passing699 Billing Python tests. It is not a complete gate and provides no immutable exit-pass credit. The original shell teardown restores generated workspace files and stops/removes only its owned stack.

The first run resolves the RF projection fixture failure: all13 actual feedback tests pass, including exact ACL/FORCE-RLS/trigger/membership restoration and foreign-key checks. The Authority browser suite passes11, fresh RF suite14 and both Tax journeys pass. Accounts then fails its first ever full browser execution at an incorrect exact-text locator: its evidence-only import displays the pending label with the existing Test-evidens prefix rather than Tax's standalone submission label. Import200 was already verified. The browser now scopes to the Accounts row and checks both the complete pending text and exact imported TT02 reference.

The second run passes import, permission, manual evidence, source-history and audit assertions, then exposes another test expectation mismatch: the workspace renders a connection error for failed Accounts reads, not the internal source helper's detailed message. The fixture records only path/status for the injected503. The corrected browser proves that the current company's read failed, the Status panel shows Feil, and the Accounts row has no stale test evidence or reference. Original authorization, persistence, history, audit and egress assertions remain.

The final focused run passes all41 Accounts SQL cases, contraction, zero blocking advisor findings (50 performance warnings), and the complete26-second real owner browser, including retained-JWT authorization revocation. The focused client/workspace/fixture models pass62 with one existing optional DB test skipped. Prior failed runs remain original separate transcripts. No application, UI copy, authority behavior or SQL changed for these browser locator corrections.

Both original independent RF projection correction reviews at5915af48 pass and are adopted unchanged, with their isolated probes and source bindings. The current browser correction still requires independent review. Complete immutable gate pair and protected integration remain pending; no provider egress, hosted migration, genuine filing, production activation or paid operation occurred.
