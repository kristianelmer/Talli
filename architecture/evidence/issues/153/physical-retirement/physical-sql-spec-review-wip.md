Bounded Spec review — two P2 findings on the five WIP SQL files frozen at 2026-09-14T10:44:39.311708Z, above `5279d43e297d3a3c2949c61d8466ca79c20651cb`. Later working-tree corrections are excluded.

1. **SPEC-153-PHYSICAL-1 — reject canonical security drift before activation.** Cutover `20260914101805_annual_accounts_filing_cutover.sql:97–120, 138–141, 164–171` attests the public source, target rows and triggers, but not canonical owner/RLS/policy structure. With `filing_previews NO FORCE ROW LEVEL SECURITY`, frozen cutover succeeds and the real workflow SQL contract exposes another company's preview to outsider and unaccepted actors. Restoring FORCE RLS denies the outsider. Compare all six canonical structures with a clean, immutable expected structure before temporary migration access or activation.

2. **SPEC-153-PHYSICAL-2 — attest the original public shell before rollback reactivation.** Full rollback `supabase/rollback/20260914101805_annual_accounts_filing_cutover.sql:44–76` checks empty rows and the fence, then restores latest rows without verifying public security structure. Disabling public preview RLS after normal cutover survives full rollback; authenticated outsider SELECT returns the restored foreign preview. Re-enabling RLS returns zero rows. Reject source-shell structure drift before copying or removing fences.

Both violate #132: “No exception may hide … failed authorization/RLS” and “Fail closed and return the capability to its prior single writer,” incorporated by #153's sixth criterion. Both findings remain open for these frozen bytes; the parent's new guards require separately bound follow-up.

Independent probes ran only on the explicitly authorized disposable localhost database, with synthetic identities and one outer transaction each. Both rolled back; schema, fixture, global memberships and source-schema ACL restoration checks passed. No hosted/provider action occurred.

Static review covered six-family classification/FKs, one-writer fences, four dependency rebindings, role restoration, latest-row replay, Archive preservation, contract indexes and lock ordering. The exact-hash retained 11-phase latest-state/recutover proof and three rejection cases pass; earlier broad cutover access evidence has an older cutover hash and is historical only. I did not rerun concurrency or all lifecycle scenarios. No additional confirmed defect; no full-stage exit, browser, provider, production or immutable-gate credit.
