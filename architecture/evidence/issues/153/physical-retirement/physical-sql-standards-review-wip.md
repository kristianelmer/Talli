Bounded Standards review: frozen #153 physical SQL WIP against `5279d43e297d3a3c2949c61d8466ca79c20651cb`. The five reviewed files were frozen at 2026-09-14 10:44:45 UTC; exact paths, SHA-256 values and private copies are recorded in the accompanying JSON. Later working-tree fixes are excluded.

**P2 — STD-153-PHYSICAL-1: reject changed shell triggers before reactivating the legacy writer.** In `supabase/rollback/20260914101805_annual_accounts_filing_cutover.sql:66–68`, full rollback disables every current noninternal public trigger. Lines 165–170 restore only names from the original inventory. If another trigger was added after cutover, rollback succeeds with that trigger left disabled; an altered definition under an original name also survives. This can suppress a later validation or evidence hook when public writes resume. Compare the complete shell structure, including trigger names, definitions and modes, to the preserved inventory before disabling/copying/reactivating it. This is a rollback behavior-preservation defect under ADR0013’s deterministic migration and rollback/recutover requirement (lines 40–44), rather than a Fowler heuristic. The consequence is established by source inspection; I did not run a database reproduction. Parent reports a correction in later WIP, outside these bindings.

Related independent Spec probes reproduce missing target FORCE RLS at cutover and disabled public RLS at full rollback on the same hashes (SPEC-153-PHYSICAL-1/2). Their scripts/results are bound separately; Standards did not execute them. These reinforce the need to attest both schemas before activation.

Otherwise, the reviewed paths use finite owner roles, temporary privileges restored with original membership options, owner-scoped dependency ports, phase/table locks, positive provenance and six-family reconciliation. The contract index inventory is insert-once and rejects mismatched definitions/hashes. No additional heuristic finding is warranted.

I inspected, but did not execute, the rollback-only synthetic SQL producers. The 11-phase latest-state result matches all five frozen hashes and covers a changed existing comment plus a new evidence row, full rollback and recutover. The three rejection cases match the frozen cutover. Earlier standalone cutover/contract receipts bind older SQL and are retained as historical evidence only. None establishes committed concurrent execution, hosted behavior, a complete gate or stage exit. My preliminary same-ID concern was withdrawn: the preserved source primary key includes the payload hash.
