import{readFileSync,writeFileSync}from'node:fs';import{buildAnnualAccountsPayload}from'./apps/web/app/lib/annual-accounts.ts';const cases=JSON.parse(readFileSync(process.argv[2]));const out=[];for(const c of cases){let value=buildAnnualAccountsPayload(c.input);const special=[];function visit(v,p=[]){if(typeof v==='number'&&(!Number.isFinite(v)||Object.is(v,-0)))special.push({path:p,kind:Object.is(v,-0)?'-0':String(v)});else if(v&&typeof v==='object')for(const[k,x]of Object.entries(v))visit(x,[...p,k]);}visit(value);out.push({id:c.id,output:special.length?{value,numericSpecials:special}:{value}})}writeFileSync(process.argv[3],JSON.stringify(out,null,2)+'\n');