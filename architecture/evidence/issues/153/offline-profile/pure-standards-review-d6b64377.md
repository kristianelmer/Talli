One actionable finding (P2); no additional Standards or heuristic findings.

**Preserve accepted deep source snapshots — `public.py:9–12`.** `_freeze` recursively copies every nested source value. Adding a 600-level ignored JSON array to the existing supported Ledger-entry fixture makes `AnnualAccountsSource(...)` raise `RecursionError`, before calculation. The exact predecessor returns its unchanged 21-field candidate. The public contract permits `Mapping[str, object]` snapshots and documents no depth limit. Replace call-stack recursion with an iterative immutable copy, preserving copied mappings/tuples and rejecting actual cycles explicitly, and add a predecessor-bound regression. This is a concrete robustness/preservation defect against the advertised source contract (`MODULE.md:23–26`) and ADR0013’s characterization-preserving migration requirements; it is not a Fowler heuristic or an observed HTTP failure.

Reviewed immutable `f8ec6c52e4e21c52362fadacb852953aab7c961a...d6b64377fbef401cdafcf3c5bc59cece8f816f61`. Independently ran all 254 committed public-contract conformance checks successfully in a private extraction and reproduced all fourteen legacy CLI captures byte-for-byte with Python 3.12.12. The deep-source predecessor probe uses pinned Node 24.20.0; both producer files and the red transcript are retained privately.

All nineteen manifest references and four adopted independent review originals match. Public `__all__` matches the declared exports; the module uses local policy/stdlib only, owns no active storage or provider port, and correctly distinguishes caller-supplied facts from authorization/completeness attestations. Registry, frozen baseline, historical characterization and original pending criteria are unchanged. The offline profile’s separate public-contract migration remains pending; no second owner is accepted at exit.

Architecture PASS is inspected from its bound evidence, not rerun. No SQL, caller, provider, shared-source or database changes were performed. This bounded pure review grants no stage-exit credit.
