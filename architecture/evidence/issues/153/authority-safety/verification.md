# Accounts authority safety and deployment evidence

Base: eb7d312054c32a8e94dd9d0c8bf9f22dcf9a12fa. Full stage exit remains pending.

Both whole-stage review axes independently reproduced two inherited P2 failures: lost create/lock responses could replay consequential operations (STD-153-REPLAY-1), and malformed HTTP200 validation could authorize lock (STD-153-VALIDATION-1). Those behaviors conflict with #132 despite predecessor parity. Original reports and probes remain unchanged.

Create and lock now persist operation identity, instance binding and time before the effect. Success clears the marker only with its saved checkpoint. Lost/malformed responses, interruption and checkpoint failure leave an unresolved marker; rerun stops before provider construction. Legacy ambiguous states require reconciliation too. No automatic new evidence path or repeated create/lock resolves an unknown outcome. Confirmed lock resumes by reading submission state after a lost handoff. Known data-ID uploads preserve same-payload PUT behavior. The evidence guard deliberately changes ambiguous failure status to reconciliation_required with a nonretryable error.

Validation requires an actual list (direct or validationIssues), explicit recognized severities, and positive signing task before handoff. Official Altinn numeric severity1 blocks;2–5 and existing string aliases retain documented nonblocking meaning. The primary model and enum are pinned by URL/revision/hash in altinn-validation-primary-sources.json. The original valid XML, payloads and HTTP method/body/order are unchanged. Three adapter methods have intentional validation/signing safety corrections; earlier transport identity claims remain historical.

275 focused authority/rehearsal/transport/CLI/ownership tests pass. New regressions include lost responses, malformed response objects, interruption, failed success checkpoint, legacy ambiguity, HTML/null/wrong-shaped validation, numeric errors, recognized soft/empty validation, unknown signing states, and confirmed-lock read-only recovery. Original traces remain immutable; comparison removes only added journal metadata/pending saves and explicitly asserts the changed create/lock error cases. The first red runs, intermediate harness errors and final passing logs are retained.

The full eb7 gate passed schema, credential scan, typecheck, architecture, backend/web/API boundaries, both builds, boundary smoke, launch rehearsal and dependency audit. Database isolation failed on a stale static Archive source inventory before final owner browser lanes. Its original transcript is preserved. The corrected inventory test maps the five original Archive trigger scopes to Accounts ownership and retains the complete inventory assertion; all 16 tests pass.

Deployment compatibility is separately proven by 17 actual local legacy-PostgREST/current-FastAPI/SQL checks across predecessor, expansion, cutover, contract, contract rollback, full rollback and recutover. All five evidence generations survive. Seven further checks run the current generated consumer against exact predecessor5b FastAPI HTTP: missing routes reject, lookup/ack do not hide raw404 as absence, and source composition remains unavailable. The old-app first harness incorrectly polled disabled OpenAPI; corrected health/live polling passes. Both proofs use fixture credentials, not genuine authority actions or real Auth/TOTP; their owned containers/databases/logins/server are removed and roles restored. Parent source bindings and Spec verification bind exact eb7 consumed bytes.

No hosted change, provider call, production activation or successor-stage credit is claimed. Real full owner/browser integration, the complete two-revision exit pair and protected integration remain pending.
