Two P2 hard-rule findings; implementation readiness is not yet clear. Reviewed the whole #153 delta from protected baseline5b74340ca75f215b43d754c6bf0fc49ef5974b0b to immutable eb7d312054c32a8e94dd9d0c8bf9f22dcf9a12fa, using ADR0011–13, #132’s approved envelope, module/ownership documentation and the Fowler heuristic baseline.

**STD-153-REPLAY-1 — Reconcile ambiguous authority effects before repeating them.** `apps/backend/src/talli_backend/modules/annual_accounts_filing/rehearsal.py:110–115,133–145` records create/lock completion only after the response. Losing that response persists failed_retryable with no instance or locked=false. My two-invocation public-contract probe repeats create_instance and lock_for_signing against the same stored evidence, without reconciliation. Persist an intent/unknown checkpoint before each consequential call and prohibit repeating unresolved effects. This violates #132’s explicit prohibition on blind retries of ambiguous provider effects.

**STD-153-VALIDATION-1 — Reject absent or malformed validation evidence.** `apps/backend/src/talli_backend/adapters/annual_accounts_authority.py:72–81` defaults malformed/missing issues to an empty list. Through the actual adapter and public prepare contract, HTTP200 HTML and a non-array validationIssues value both yield hasErrors=false, trigger the process/next PUT and return signing handoff. Preserve legitimate empty-array/wrapper responses but reject unusable validation before locking. This conflicts with #132’s fail-closed filing requirement and ADR0011’s consequential adapter boundary. Both findings preserve inherited predecessor behavior; equivalence alone does not satisfy the mandatory safety envelope. Neither probe contacted a provider.

STD-153-DEPENDENCY-1 is closed: the new AAL2 control uses a timestamp86,400 seconds old alongside AAL1 rejection. Original reports remain bound.

Independent checks passed:2,468 backend pure/API/authority-fake tests,101 generated-client/consumer/Archive tests, and architecture validation. All594 references in22 manifests resolve to current or historical committed bytes;93 entry source hashes match the baseline. Both published log transformations change only four synthetic header markers each; private originals and the earlier committed log are preserved, and credential scanning is unchanged. All149 prior OpenAPI paths and418 schemas are unchanged. Frozen compatibility baseline and predecessor SQL artifacts remain unchanged.

No other actionable ownership, immutable-contract, retirement or schema-lifecycle defect was established. SQL/browser/deployment proofs remain separately bounded; full gates, protected integration and stage exit receive no credit here.
