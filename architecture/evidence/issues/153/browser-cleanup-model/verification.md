# Browser cleanup catalog model follows Accounts ownership

The exact10e6698efdcad2726f5ca9f7dcdb7352317ceb11 complete gate passed all phases preceding database isolation. In that lane the real owner-onboarding browser scenario passed, but seven existing cleanup model tests failed because their catalog treated undeclared Accounts relations as present and lacked the Accounts schema owner. No Accounts browser lane or complete exit credit is claimed. The original complete transcript is committed at architecture/evidence/customer-ready-gates/10e6698efdcad2726f5ca9f7dcdb7352317ceb11.log.

The fixture catalog now explicitly models absent Accounts relations in earlier phases, present canonical Accounts tables during expansion, and final removal of all six generic tables during contract. The exact schema owner is declared. Original77-family coverage remains before each phase-specific mapping, original role/ACL/FORCE-RLS/trigger restoration assertions remain, and final Accounts deletion order is checked. Three new negative models reject missing Accounts family, partial generic retirement and non-contracted phase before any source deletion. Production cleanup implementation is unchanged.

The tight pre-fix invocation reproduces seven failures. After correction all15 focused cleanup checks pass. The broader fixture/process suite passes27 checks with one real database test explicitly skipped because that targeted invocation has no local Supabase runtime. The complete gate will run that mandatory test against its owned local database. The282 Accounts authority tests also pass on the root worktree after fast-forwarding the exact isolated journal-ownership commit ee0a6d862759c47ecdb0679300280095e8324359. Independent correction reviews and the complete gate pair remain pending. No hosted/provider/production effects occurred.
