Spec review: **STD-153-READ-1 closed; one new P2**, shared with Standards as **STD-153-READINESS-1**, in `af5c6523f081707fc08818919717371af6dd4ec9...d36a6af7d4a8f3cff49b27f07e9570dd045fac72`.

**Reject malformed Annual confirmations before readiness computation** (`apps/backend/src/talli_backend/main.py:1142–1156`). GH-153-A3 requires states to “remain fail closed and auditable”; A1 requires outputs identical to the existing behavior. Starting from the frozen supported readiness input, set `annualData.confirmations={}`. The new authenticated endpoint returns **200 with `issues:[]`**. The actual unchanged Node `aarsregnskapIssues` throws `TypeError: annualData.confirmations.includes is not a function`. The request validator checks Ledger warning collections but not this consumed Annual collection. Reject the invalid collection at the HTTP boundary while preserving the frozen pure rules. Valid array control matches; numeric confirmations already return 422. This is a false-clear preview/input-validation defect, not demonstrated durable authorization or provider access.

The previous deep-response failure is independently closed: the original probe now returns 200 at depth 600. The committed actual preview/workspace tests preserve complete content at depths 1/600/1200. Both routes still reject invalid typed row status with 503. Ordinary validated alias, UUID, timestamp and JSON-value formatting tests pass.

Independent verification: **81 focused Python tests**, eight additional authentication/model-validation cases, six generated-client transport/scope cases, and four old-Node/new-HTTP adversarial comparisons. Authentication precedes policy computation; no persistence session is used by readiness. All **24 manifest hashes** verify. All **157 previous paths and 432 schemas** remain structurally unchanged; one path/four schemas are added. Frozen readiness, public contracts, calculation, original fixtures and six pending criteria are byte-unchanged.

No other actionable finding. Caller facts and Corporate blockers are previews, explicitly not durable source completeness or filing authorization. Web cutover, source handoff, lifecycle/Archive work and full-stage exit remain pending. Uncommitted corrections and web scaffold are excluded. No database, browser, provider or shared-file mutation; no full-stage credit.
