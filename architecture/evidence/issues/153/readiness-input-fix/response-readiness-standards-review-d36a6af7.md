**STD-153-READ-1 closed; one new P2 boundary-validation finding.** Reviewed `af5c6523f081707fc08818919717371af6dd4ec9...d36a6af7d4a8f3cff49b27f07e9570dd045fac72`.

**STD-153-READINESS-1 — validate consumed Annual facts before previewing** (`apps/backend/src/talli_backend/main.py:1142–1156`). The new request leaves Annual facts as `dict[str, Any]`, checking only Ledger `risk_flags`. Starting with the frozen supported case, Boolean `general_meeting_approved=False` correctly returns `general_meeting_not_approved`; changing it to the malformed string `"false"` returns **200 with no issues**. Object-valued `annual_full_time_equivalents` and `confirmations` likewise produce clear previews. Exact predecessor feedback code throws TypeError for `confirmations={}`; the new HTTP result silently clears it. Thus malformed transport values silently become valid-looking domain facts. Validate consumed nested Annual shapes at the HTTP boundary (including Boolean approval, numeric/null equivalents and the confirmation list) and reject malformed values with 422. Preserve the pure legacy JavaScript coercion behavior separately. This is a transport-contract robustness finding under ADR0012's governed backend boundary and CONTEXT's filing-validation semantics, not a Fowler smell or durable-authorization bypass. The route is correctly documented as a preview.

The serialization correction is sound in the reviewed scope: typed models still validate before explicit Response construction; iterative traversal retains aliases, UUID/date formatting and nested JSON, and rejects cycles. Independent rerun of the original shallow/600-depth probe now returns 200 for both. The six added route/depth regressions cover workspace and preview through depth1200.

Independently ran **81 focused Python tests** and **two existing generated-client regressions**, all passing, using the immutable private archive with Python3.12.12 and Node24.20.0. The additional malformed-input probe reproduces the new finding. Verified 24 artifact hashes, six exact original review/probe adoptions, unchanged prior OpenAPI objects, and additive one path/four schemas. Pure policy, registry, original criteria and SQL remain unchanged; generated readiness responses check company/year scope. No shared mutation, database/provider operation or full-stage credit. Untracked web work is excluded.
