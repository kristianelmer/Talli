Spec review: **one P2 finding**, shared with Standards as **STD-153-READ-1**, at `af5c6523f081707fc08818919717371af6dd4ec9` against `e6f8e889852e9c26057d80f75930dc5a34ad5729`.

**Deep retained JSON fails at the HTTP response boundary** (`apps/backend/src/talli_backend/main.py:10343–10350`; preview uses the same model serialization). GH-153-A1 requires “Supported annual-accounts cases produce outputs identical to current schema and TT02 evidence.” The new public contract also promises “complete, immutable Accounts rows” (`MODULE.md:135–138`). Independently rerunning the Standards probe on the pinned source confirms that valid 600-level ignored issue metadata survives standard JSON round-trip, immutable projection and thaw, but the real FastAPI workspace response returns 500; the shallow control returns 200. The response serializer fails after endpoint return. Preserve nested row content through that final boundary and cover actual HTTP serialization. The parent's uncommitted correction is excluded from this finding disposition.

No additional actionable defect found in this slice. Accepted membership, applicable owner/reviewer checks and fresh MFA remain Company Access decisions; SQL business entry points are inactive until cutover/contracted. Accounts TT02 import inserts one pending evidence row per request. Existing later web Audit remains unchanged; no Tax combined submission, replay deduplication or atomic Audit was introduced.

Independent checks: **62 Python tests, two generated-client tests, 13 additional client shape/scope/link cases**, and five original-Node versus new-HTTP import probes (depths 0/100/600/1000/1200) pass. All **43 manifest hashes** verify. Structural OpenAPI comparison preserves all **149 previous paths and 418 schemas**, adding eight paths and fourteen schemas. Original importer bytes match the protected stage baseline. Original six criteria remain unchanged and pending.

Inspected the retained 47 synthetic SQL cases and 36 denied execute privileges, including exact SQL bindings; did not rerun them or use the SQL slot. Their rollback-only phase toggle is not real cutover, JWT-ceremony or concurrency proof. Web cutover, source handoff, migration lifecycle/Archive integration and full exit remain explicitly pending. No shared files, databases or provider state changed. No full-stage credit.
