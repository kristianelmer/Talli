Bounded Spec review of `1e38067282e144996b455e428c267f6be3534421...900472a0ecfbf5e8d7fc4996f4f641e33575103c`: prior finding closed; one new shared P2.

**STD-153-SOURCE-1 CLOSED.** The projector now accepts null or an array of strings and rejects other document-reference shapes. Independently replayed the original three malformed stored-value fixtures and empty-array control against the pinned public contract: malformed values fail unavailable and the array remains unchanged. Independently ran the source suite: 29 pass, including authenticated HTTP 503 regressions and supported null/text-array controls.

**STD-153-BINDING-1 OPEN — enforce no administration on an existing binding.** `supabase/contract-migrations/20260914112549_annual_accounts_filing_backend_binding.sql:17` grants `INHERIT false, SET true` but omits `ADMIN false`. Actual rollback-only SQL demonstrates a fresh membership is correct, while a pre-existing `(admin=true, inherit=false, set=true)` membership remains administrative after the artifact succeeds. This contradicts the committed restricted-runtime requirement, “without inheritance or administration,” and permits delegation of the workflow role. Explicitly clear administration or reject unexpected prior membership; cover that rerun state. Schema, fixture and global membership restoration passed; SQL slot released.

Browser integration is correctly placed after Accounts activation. The new journey exercises AAL1 denial, pending-only TT02 import, manual evidence/permission, source completeness/scope/outage, role revocation and retained external Audit. It contains no signing/provider action. Retained Annual/Tax fixtures use canonical rows and assert retired generic tables are absent. Cleanup adds finite Accounts families, deletes children first and retains existing owner/ACL/trigger/FK restoration mechanics. I found no additional confirmed regression; browser execution remains explicitly unproved.

Five independent fixture-safety tests pass. All 36 manifest hashes and eight adopted original Spec artifacts verify byte-exact; original criteria, OpenAPI, generated client and compatibility registry are unchanged. Parent-run 23 lifecycle/20 fixture tests and architecture logs are retained evidence, not independent reruns. No shared source edit, competing server, hosted/provider action or full-stage/gate credit.
