Bounded Spec review of `9ba2a33a2214b398ca0d62e1bad6bd3fc1674fe3...1e38067282e144996b455e428c267f6be3534421`: one shared P2 finding; no stage-exit credit.

**STD-153-SOURCE-1 — validate retained document-reference collections before declaring complete history.** `apps/backend/src/talli_backend/modules/annual_accounts_filing/source_facts.py:148` applies `tuple(value or ())` to opaque stored JSON. A stored string becomes character IDs, an object becomes key IDs, and numeric zero becomes an empty list, while `history_coverage.status` remains `complete`. Independently reproduced all three using actual permitted legacy JSONB rows, followed by exact committed expansion/cutover/contract and the real source SQL/public projection; an empty-array control remains correct. Standards separately reproduced the HTTP consequence. Reject malformed collection shapes rather than inventing or dropping source references. This violates the approved #192 requirement: “Preserve evidence identity, source version, timing, attribution and company/year,” and #153's fail-closed evidence requirement. Finding remains open on this commit.

Eight other independent SQL checks passed: populated and positively covered empty years; reviewer, unaccepted-owner and outsider denial; unknown production-labelled history; missing retained submission identity; and rejection of read-committed access. Three generated-client tests passed with zero skips. Probe-only canonical mutations used a temporary owner policy removed before reading; this is a synthetic fixture technique, not claimed business authorization. Every transaction rolled back, with schema/fixture/global membership restoration verified. The initial mutation harness hit the intentionally absent UPDATE policy; its failed transcript is preserved separately.

The source is read-only, company/year-bound and conservatively production-disabled. Coverage requires recorded extent, inventory/reconciliation/fences/mode checks and retained IDs; absent coverage is unavailable. Existing local statuses remain test/simulation or unknown, without external acceptance or monetary classification. This block certifies neither Corporate close nor signed-artifact prerequisites.

Verified all 39 manifest hashes, capture SQL bindings and byte-exact prior Spec adoption. All 158 existing OpenAPI paths and 436 schemas are semantically unchanged; one path/seven schemas are added. Parent-run projector/lifecycle/typecheck/architecture evidence remains distinct from these independent checks. No additional confirmed defect, provider/hosted/browser action, full-stage acceptance or successor credit.
