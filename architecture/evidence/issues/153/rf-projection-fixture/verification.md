# RF fixture projection retirement

The complete gate at 006469373fc79f16c20dfec75bbc0d3ecd51ff6c passes Accounts41 SQL cases, final topology activation/advisors and the real annual owner browser. It then fails two RF feedback tests because their fixture transaction still declares retired public.filing_review_comments. The original full transcript remains a failed gate with zero exit credit. The subsequent fresh RF browser has the same stale fixed projection inventory and cleanup loop.

A focused regression drives the original rfFixtureTransaction and its actual finite authority helper with deterministic catalog results. The retired phase and three invalid-topology controls fail before correction. A shared test-only selector now returns the six public projection tables only while they exist; their absence requires complete retirement, explicit Accounts contracted state and all six owned Accounts families. Unexpected relation kinds, partial retirement, missing owned families and wrong phase fail closed. Accounts rows receive no fixture authority. RF retains its original owned tables and all existing authority-restoration/foreign-key checks.

Both RF fixture callers select the current projection inventory. Fresh RF cleanup deletes selected company-scoped mirrors before canonical openings while mirrors exist; after retirement it deletes no absent generic table. The prior two cleanup controls remain, with two contracted-phase success/failure controls added. The focused combined suite passes52 tests with one existing optional real-database test skipped because no database was supplied. This is model/fixture evidence; a dedicated isolated Supabase run of final feedback and filing browser lanes is pending.

The two original independent role-creation correction reviews at00646937 and their original probes/bindings are adopted unchanged. Both axes pass with no findings; their catalog-stub probes and inspected41 SQL results are distinguished from full runtime execution. This fixture correction still needs its own independent review and the mandatory complete immutable gate pair. No hosted/provider/production action occurred.
