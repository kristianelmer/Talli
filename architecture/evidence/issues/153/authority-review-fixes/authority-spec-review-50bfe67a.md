Spec review: **two P2 findings** at `50bfe67a17faf3bab714a51372a85d7e16886671`, compared with `57df4221ba8341ae2b7c5e9942eb946e2e557088`.

1. **SPEC-153-AUTH-1 — preserve false-like Annual defaults** (`authority_tools/annual_accounts_payload.py:8–9`). #153 A1 requires “Supported annual-accounts cases produce outputs identical to current schema and TT02 evidence.” With the unchanged valid fixture, `annualData: false`, `0`, `-0.0` or `""` produces original XML and empty feedback through the actual predecessor Node subprocess. The new mapping passes the scalar to the typed source and returns `Local authority payload generation failed.` This is reachable from the rehearsal case file. Preserve the original false-like default at the mapping boundary.

2. **SPEC-153-AUTH-2 — reject malformed Ledger collections** (`authority_tools/annual_accounts_payload.py:10`). A1 and #132 require preserved output/error characterization. Direct fixed-payload inputs `ledgerEntries: {}` or `""` previously fail; the new Python path silently iterates them as empty and emits zero-balance XML with no feedback. Validate collection shape while retaining missing/null defaults. The outer rehearsal already normalizes these values; this finding concerns the migrated fixed payload boundary.

Independent private checks: **226 tests passed**; all **17 predecessor traces** and **67 Node payload captures** reproduced byte-for-byte. A separate 195-case payload corpus exposed the gaps; ten actual predecessor-subprocess controls confirmed both. Deep completed replay controls at 100/400/600 levels matched. All 40 manifest hashes and four adopted reviews verified; 13 HTTP method bodies remain unchanged apart from the relocated import. Credentials retain their pre-try boundary; checkpoint exceptions, receipt GET continuation and completed replay preserve the tested ordering. Authority response normalization remains frozen; no production filing authorization is inferred from it.

All six original criteria remain pending. No shared mutation, database/provider operation, complete workflow or stage-exit validation was performed. Exact source, probe and result hashes are in the companion JSON.
