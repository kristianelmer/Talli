Spec review: **one P2 finding** at `04e682b69e5426408932233d05328134e8e997f8`, compared with `61c9c93add0cfd0011bd55e8f71200eaf8a76a43`.

**SPEC-153-EXPAND-1 — quarantine mismatched opening-setup provenance** (`20260914090244_annual_accounts_filing_expand.sql:125–138`). The approved expansion requires positive company/year ownership; its verification states: “Every remaining row must positively match Accounts and matching referenced company/year/obligation.” The classifier never checks non-null `setup_id` on previews or submissions. Their retained FK checks only ID existence.

An independent probe restored the pinned entry dump into a separate disposable clone and executed the exact committed migration. With original legacy triggers enabled, a company-A/2025 preview referencing company-B/2025, a company-A/2025 preview referencing company-A/2024, and a submission referencing company B were all classified/copied as `accounts`; quarantine count was zero. Six count/hash reconciliations nevertheless passed. Matching and null setup controls also copied, as expected.

Validate retained setup company/year through the source owner during migration, preserving null references. Propagate a quarantined preview to its submissions, overrides and comments; otherwise rebinding their FK would fail after excluding the parent. This concerns classification of inert copied data, not a demonstrated active-writer or authorization bypass.

The independent transaction preserved legacy rows and restored migration memberships, then rolled back all fixtures/schema and global memberships. The separate clone was dropped after confirming zero sessions. Initial fixture setup hit the canonical RF write policy; the corrected synthetic seed used a temporary INSERT grant to the existing local bypass-RLS runner, rolled back afterward. Source triggers were never disabled.

All **29 manifest hashes**, four adopted reviews and original pending criteria verified. Recorded exact-SQL evidence covers seven rows/six families, 60 denied-access probes, seven classifiers and two quarantined rows; those lanes were inspected, not rerun. The independent probe also counted 37 inventoried routines. The earlier empty probe has a historical SQL hash and is not current-artifact execution credit. Advisors report 46 findings/zero blocking/zero Accounts. Cutover, rollback contracts and full-stage acceptance remain pending.
