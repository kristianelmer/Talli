Standards review: **one P2 finding** in `e6f8e889852e9c26057d80f75930dc5a34ad5729...af5c6523f081707fc08818919717371af6dd4ec9`.

**STD-153-READ-1 — preserve deep JSON through the HTTP response boundary** (`apps/backend/src/talli_backend/main.py:10343–10350`; the preview response uses the same wire model). The owned contract promises complete immutable predecessor rows (`modules/annual_accounts_filing/MODULE.md:133–138`), and ADR0012 requires the governed HTTP contract. A synthetic valid preview containing 600 levels of ignored JSON metadata inside an issue passes JSON round-trip, immutable `AnnualAccountsFilingRows` validation and `annual_accounts_json_wire`, but the real FastAPI workspace response returns **500 INTERNAL_SERVER_ERROR**. Pydantic raises `ValueError: Circular reference detected (depth exceeded)` while serializing after the endpoint returns, outside its `except ValidationError`. The identical shallow control returns 200. There is no corresponding depth constraint in the owned JSON contract. Preserve supported nested row data through response serialization and add an actual HTTP regression; merely changing the immutable copier does not fix this failure. This is a documented contract-preservation gap, not a heuristic smell or demonstrated database authorization bypass.

Otherwise, the bounded ownership structure is consistent: Accounts owns normalization/public persistence ports; the technical adapter composes verified authentication without inheriting Ledger writes. Fixed SQL executes through restricted roles, Company Access owns authorization, and phase gates prevent reading or mutating inactive copies. TT02 remains one insert with pending status and subsequent web Audit explicitly pending cutover.

Independently ran **62 focused Python tests and two generated-client tests**, all passing, in an immutable private archive with Python 3.12.12 and Node 24.20.0. The additional HTTP probe reproduced the finding. All manifest/adoption hashes and additive OpenAPI structure verified. Retained 47 SQL/36 execute-denial cases were inspected, not rerun. No shared source, database, role, browser or provider mutation. Original criteria and compatibility scope are unchanged; no full-stage credit.
