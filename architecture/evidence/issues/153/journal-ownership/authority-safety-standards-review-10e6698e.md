One remaining P2 hard-rule finding. STD-153-REPLAY-1 and STD-153-VALIDATION-1 close for the reproduced sequential failures.

**STD-153-JOURNAL-1 — Hold exclusive ownership of the evidence journal across the rehearsal.** `apps/backend/src/talli_backend/modules/annual_accounts_filing/rehearsal.py` reads/checks prior evidence before awaiting provider construction; its later pending writes are unconditional. `apps/backend/src/talli_backend/adapters/local_annual_accounts_rehearsal.py:23–27` provides plain file reads/writes without a lock or compare-and-set. Two simultaneous actual CLI runs using one real temporary evidence file both pass the initial guard, create and lock separate instances, and return locked_for_person_signing. The file retains only the last instance, losing the other effect’s journal identity. This conflicts with #132’s single-writer, idempotency and reconciliation envelope. Acquire per-evidence exclusive ownership before reading and retain it through effects/checkpoints; reject competing runs before credentials or provider effects and release on failure/interruption. The reproduction uses the exact CLI/local file adapter with a fake provider, not a hypothetical interleaving. Parent acknowledged the finding.

Reviewed immutable eb7d312054c32a8e94dd9d0c8bf9f22dcf9a12fa...10e6698efdcad2726f5ca9f7dcdb7352317ceb11 under the existing Standards/ADR/Fowler baseline. Original probes now raise reconciliation-required or invalid-validation errors. Independent additional probes confirm durable pending markers survive persistent post-effect save failure and interrupted success writes; failed intent persistence starts no effect. Malformed responses and numeric Error block locking; valid empty validation remains supported. Confirmed-lock recovery and explicit signing-task checks preserve safe read-only continuation.

Independent validation:275 focused Python tests,16 Archive inventory tests and10 additional durability/validation controls passed. All46 manifest hashes,25 adopted private artifacts and both pinned Altinn primary-source hashes match. Historical traces retain exact successful effects and normalize only declared journal additions, with two explicitly asserted error changes. The original gate failure and limited local deployment proofs remain honestly labelled. No SQL, Docker, provider or full-gate execution occurred; full-stage/integration acceptance remains pending.
