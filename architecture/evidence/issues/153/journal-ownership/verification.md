# Exclusive Accounts rehearsal journal ownership

Both independent review axes close the earlier sequential retry and malformed-validation findings at10e6698efdcad2726f5ca9f7dcdb7352317ceb11, but establish residual P2 STD-153-JOURNAL-1. Actual concurrent CLI invocations sharing one real JSON journal create and lock two separate instances and retain only one. Original reports, mock-provider reproductions and independent output bindings are unchanged here.

The Accounts workflow now requires exclusive evidence ownership before case/evidence reads and holds it through every await and final checkpoint. Its registered local adapter uses a nonblocking POSIX flock on a canonical-path sidecar. A persistent sidecar retains the lock across atomic JSON replacement; it is never unlinked. A concurrent invocation receives nonretryable ANNUAL_ACCOUNTS_REHEARSAL_IN_PROGRESS before credentials. Descriptor closure releases ownership on success, exception, interruption and process termination. Pending operation evidence survives process death, preserving the existing reconciliation requirement. The lock coordinates this local filesystem; choosing another journal does not reconcile an authority outcome.

Seven new regressions fail before the fix and pass afterwards: actual concurrent CLI calls with identical and directory-aliased evidence paths; four exception/cancellation release controls; and a real child process proving exclusion across atomic replacement and release after termination. The focused regression command runs 282 tests successfully. The original successful effect traces remain unchanged. An isolated in-memory test IO explicitly supplies a no-op ownership context; the real adapter is always exercised by the concurrency tests.

The first private archive architecture harness was invalid: symlinked feature paths and committed receipts produced errors, and its wrapper incorrectly examined violations instead of errors when setting exit status. Its full output is retained without success credit. A proper detached Git worktree at the exact base with only the seven changed paths passes the actual architecture CLI; all282 focused tests also pass there. No scanner rule or architecture assertion was relaxed.

The root10e full gate is still running against unchanged committed inputs. It cannot provide final exit credit while this finding remains in its source revision. This fix still needs independent review, complete immutable gate pair and protected integration. No provider, hosted database or production action occurred.
