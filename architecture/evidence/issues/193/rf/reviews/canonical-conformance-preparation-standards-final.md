PASS — preparation only, not execution authorization.

STD-193-RF-CONFORMANCE-1 is closed. The runner now checks the complete configuration-file SHA256 before JSON parsing or credential access, and the plan explicitly binds the optional system-user external reference to null. The adapted original guard/claim-builder probe rejects changed delegation. Three actual-main mock cases also reject added external-reference values before the credential seam or authorization file.

The new document guard consumes at most 10 MiB, checks the original receipt hash immediately, and closes the original response on success, oversize, mismatch or cancellation. A changed first receipt stops before the second document GET and before artifact persistence. Successful responses retain the same bytes and MIME type for canonical parsing. No arbitrary URLs, headers or provider diagnostic bodies are persisted.

Independent verification: 32 mock tests passed, including six additional private token/file/stream checks, plus the adapted original configuration probe. All 22 source-file bindings and the configuration byte digest match the current plan. This review did not read the key file, sign a grant, call a provider, open a browser or execute the approved action.

The action remains bounded to the existing synthetic company/year/submission: two token grants, one exact dialog GET, two exact receipt GETs, no retries/redirects/filing POST/configuration or hosted writes. Attempt authorization is exclusive and checkpointed before effects; cancellation discards acquired tokens. Private receipts and the Talli-owned provenance manifest support local conformance evidence, not proof of hosted persistence or full RF/#193 completion.

Final plan SHA256: b1d60bd7cccc2b73cae0e5a5642792b6dfed9846bfa732aa5378f4bf1838d4dd. Runner SHA256: 2212b41064f5deb1b0e92408c8717431f3409077469907efc025f6515e7862b8. Original reports and reproductions remain unchanged.
