# Resumed SPEC review — 56c1f752 working-tree delta

**One P2 finding in the reviewed partial RF delta.** File hashes in the adjacent JSON bind this review to the inspected state; original reviews are unchanged.

**RF-RESTORE-01 — Reject cyclic correction lineage.** In `apps/web/app/lib/backup-restore.ts:207`, restore integrity checks only that `supersedesSubmissionId` exists. Setting the valid test fixture’s submission `s` to supersede itself produces `ok: true` and no failures. Two-node cycles likewise satisfy the existence check. The backend archive source already rejects self/cyclic chains, but a corrupted or altered archive passes the restore gate. Walk each predecessor chain and reject repeats; cover self-links and two-node cycles. This implements #193’s “complete … feedback/correction, final decision/receipt, and archive” and preservation of correction history.

The earlier loss-reduction mismatch is fixed: normalization binds exact old/new share capital without fractional share-count rounding. Terminal archive validation now requires matching immutable receipts and successful reconciliation evidence; the targeted regressions pass. No additional confirmed defect or scope creep found in the source foundation, backend archive or inspected migration/rollback.

Independent verification: **212 backend source/archive/API tests passed; 70 web/archive/schema tests passed.** No provider action. SQL was inspected; restricted-role database tests were not independently rerun.

Limits remain explicit: this is a deterministic source foundation without DB capture/application integration or production-admission changes. Full #193 acceptance, genuine-company evidence, complete gates, company tax and annual accounts remain pending.
