# SPEC review — 81a37a33

Reviewed `git diff 7d2eca51abad4bd7a1e14cc903adbf9cdb5c331e...81a37a3365d9de4bcaad16417dc0beaf444f8292`, one commit. This original review is immutable; corrections require a separate record.

1. **P2 — class/discriminator disagreement bypasses admission** (`readiness.py:132`). #193 requires “complete readiness, preview, cross-output reconciliation.” Constructing `Rf1086CashIssueEvent` from the parsed formation fixture fields, retaining `type="formation"` and setting `registration_confirmed=False`, produces **ready** and emits main code **N**. Reconciliation uses the string discriminator; rendering uses the class. Validate their agreement before dispatch so an event cannot reconcile as formation and render as unconfirmed new issuance.

2. **P2 — below-minimum loss cover is a passing case** (`readiness.py:177`). #172 supports registered loss reductions “when the facts and treatment are clear.” The new happy-path fixture reduces registered capital from NOK 30,000 to NOK 20,000 and reports **ready**. The ordinary AS minimum is NOK 30,000 ([Brønnøysund](https://www.brreg.no/aksjeselskap/aksjekapital/)). Reject the inconsistent registration assertion or require an explicit supported exception; replace this positive fixture. Preserving historical reporting must not imply supported readiness.

3. **P2 — post-loss dividends lack eligibility facts** (`readiness.py:189`). #172 says uncertainty affecting “legal approval” is a hard block. A reconciled cash issue to NOK 45,000, loss reduction to NOK 30,000, then dividend passes **ready**, with no creditor-notice or exception facts. Loss cover without creditor notice carries a three-year dividend restriction ([Brønnøysund](https://www.brreg.no/aksjeselskap/aksjekapital/nedsettelse-av-aksjekapital/)). Block/clarify this sequence until authoritative facts resolve the restriction.

Verification: 36 focused mapping/capital tests passed; all three reproductions above independently confirmed. Recovery code retains final-decision/artifact guards; no additional confirmed recovery finding. No scope creep identified.

Limitations: this is a partial RF slice. Hosted source integration, expanded conformance, genuine production evidence and all complete #193 gates remain pending, not newly introduced defects. No filing-provider interaction or credentials used. The temporary handoff path was unavailable; committed evidence, requirements and authoritative issue decisions were inspected.
