{
  "schemaVersion": "1.0",
  "purpose": "Admit only the immutable identity, request metadata, Norwegian money, income-year, date/time, risk, coded-error, clock, and identifier-generation primitives approved by issue #124; business policy, persistence, workflows, adapters, and transport types remain forbidden.",
  "sourceScopes": ["apps/backend/src/talli_backend"],
  "allowedPrimitives": ["talli_backend.shared.kernel"],
  "forbidden": ["business rules", "database repositories", "provider adapters", "web presentation", "cross-capability orchestration"],
  "allowedPublicPackages": ["talli_backend.shared.kernel"],
  "forbiddenImportPrefixes": [
    "talli_backend.shared.persistence",
    "talli_backend.shared.workflows",
    "talli_backend.shared.adapters",
    "talli_backend.shared.transport"
  ]
}
